// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { AccessData, AccessErrors, AccessResponses, AddressApprovalData, AddressApprovalErrors, AddressApprovalResponses, AdmitLineData, AdmitLineErrors, AdmitLineResponses, AppendImportRowsData, AppendImportRowsErrors, AppendImportRowsResponses, AppendStreamData, AppendStreamErrors, AppendStreamResponses, ApplyData, ApplyErrors, ApplyResponses, ApproveSourceData, ApproveSourceErrors, ApproveSourceResponses, ArtifactContentData, ArtifactContentErrors, ArtifactContentResponses, AuditBoundsData, AuditBoundsErrors, AuditBoundsResponses, AuditData, AuditErrors, AuditExportData, AuditExportErrors, AuditExportResponses, AuditExportRowsData, AuditExportRowsErrors, AuditExportRowsResponses, AuditExportsData, AuditExportsErrors, AuditExportsResponses, AuditResponses, AuthConfigData, AuthConfigResponses, BlockContextData, BlockContextErrors, BlockContextResponses, BuildExportData, BuildExportErrors, BuildExportResponses, CallbackData, CancelAuditExportData, CancelAuditExportErrors, CancelAuditExportResponses, CancelConversationExportData, CancelConversationExportErrors, CancelConversationExportResponses, CancelExecutionData, CancelExecutionErrors, CancelExecutionResponses, CancelImportJobData, CancelImportJobErrors, CancelImportJobResponses, ClaimData, ClaimErrors, ClaimResponses, ClearScheduleData, ClearScheduleErrors, ClearScheduleResponses, ClearWorkflowScheduleData, ClearWorkflowScheduleErrors, ClearWorkflowScheduleResponses, CompareCasesData, CompareCasesErrors, CompareCasesResponses, CompareResultsData, CompareResultsErrors, CompareResultsResponses, ConversationPolicyData, ConversationPolicyErrors, ConversationPolicyResponses, ConversationTurnContentData, ConversationTurnContentErrors, ConversationTurnContentResponses, CreateAuditExportData, CreateAuditExportErrors, CreateAuditExportResponses, CreateConversationExportData, CreateConversationExportErrors, CreateConversationExportResponses, CreateOrganizationData, CreateOrganizationErrors, CreateOrganizationResponses, DeclareScoringRunData, DeclareScoringRunErrors, DeclareScoringRunResponses, DeriveCohortData, DeriveCohortErrors, DeriveCohortResponses, DiffScorecardData, DiffScorecardErrors, DiffScorecardResponses, DiscardBundleData, DiscardBundleErrors, DiscardBundleResponses, EraseConversationContentData, EraseConversationContentErrors, EraseConversationContentResponses, ExecutionHistoryData, ExecutionHistoryErrors, ExecutionHistoryResponses, ExecutionTimersData, ExecutionTimersErrors, ExecutionTimersResponses, FinishTrainingRunData, FinishTrainingRunErrors, FinishTrainingRunResponses, ForgetResultData, ForgetResultErrors, ForgetResultResponses, GateResultData, GateResultErrors, GateResultResponses, GetAlertChannelData, GetAlertChannelErrors, GetAlertChannelResponses, GetAlertDeliveryData, GetAlertDeliveryErrors, GetAlertDeliveryResponses, GetAlertRuleData, GetAlertRuleErrors, GetAlertRuleResponses, GetAlertRuleVersionData, GetAlertRuleVersionErrors, GetAlertRuleVersionResponses, GetAssessmentHistoryData, GetAssessmentHistoryErrors, GetAssessmentHistoryResponses, GetBlobData, GetBlobErrors, GetBlobResponses, GetCalibrationData, GetCalibrationErrors, GetCalibrationResponses, GetCasesData, GetCasesErrors, GetCasesResponses, GetConversationDatasetRowsData, GetConversationDatasetRowsErrors, GetConversationDatasetRowsResponses, GetConversationExportData, GetConversationExportErrors, GetConversationExportResponses, GetDatasetRowsData, GetDatasetRowsErrors, GetDatasetRowsResponses, GetDerivedCohortData, GetDerivedCohortErrors, GetDerivedCohortResponses, GetEvaluationData, GetEvaluationErrors, GetEvaluationResponses, GetExecutionData, GetExecutionErrors, GetExecutionResponses, GetExperimentData, GetExperimentErrors, GetExperimentResponses, GetExportCocoData, GetExportCocoErrors, GetExportCocoResponses, GetExportData, GetExportErrors, GetExportResponses, GetHubImageData, GetHubImageErrors, GetHubImageResponses, GetImageData, GetImageErrors, GetImageResponses, GetImportBatchData, GetImportBatchErrors, GetImportBatchResponses, GetImportJobData, GetImportJobErrors, GetImportJobResponses, GetImportManifestData, GetImportManifestErrors, GetImportManifestResponses, GetLabData, GetLabErrors, GetLabMeasurementData, GetLabMeasurementErrors, GetLabMeasurementResponses, GetLabResponses, GetLabVersionData, GetLabVersionErrors, GetLabVersionResponses, GetMetricsData, GetMetricsResponses, GetModelData, GetModelErrors, GetModelResponses, GetOptimizationData, GetOptimizationErrors, GetOptimizationResponses, GetPipelineRevisionData, GetPipelineRevisionErrors, GetPipelineRevisionResponses, GetProjectData, GetProjectErrors, GetProjectResponses, GetPromptData, GetPromptErrors, GetPromptResponses, GetPromptVersionData, GetPromptVersionErrors, GetPromptVersionResponses, GetRecordingData, GetRecordingErrors, GetRecordingResponses, GetResultData, GetResultErrors, GetResultResponses, GetRubricData, GetRubricErrors, GetRubricResponses, GetRunData, GetRunErrors, GetRunEventsData, GetRunEventsErrors, GetRunEventsResponses, GetRunResponses, GetScheduleData, GetScheduleErrors, GetScheduleResponses, GetScorecardData, GetScorecardErrors, GetScorecardResponses, GetScorerCatalogData, GetScorerCatalogErrors, GetScorerCatalogResponses, GetScoringRunData, GetScoringRunErrors, GetScoringRunResponses, GetTrainingRunData, GetTrainingRunErrors, GetTrainingRunResponses, GetWorkflowData, GetWorkflowDefinitionData, GetWorkflowDefinitionErrors, GetWorkflowDefinitionResponses, GetWorkflowErrors, GetWorkflowExecutionData, GetWorkflowExecutionErrors, GetWorkflowExecutionResponses, GetWorkflowResponses, GetWorkflowScheduleData, GetWorkflowScheduleErrors, GetWorkflowScheduleResponses, HeartbeatData, HeartbeatErrors, HeartbeatResponses, ImportImagesData, ImportImagesErrors, ImportImagesResponses, IngestData, IngestErrors, IngestResponses, InvitationsData, InvitationsErrors, InvitationsResponses, InviteData, InviteErrors, InviteResponses, ListAlertDeliveriesData, ListAlertDeliveriesErrors, ListAlertDeliveriesResponses, ListAlertRulesData, ListAlertRulesErrors, ListAlertRulesResponses, ListApprovalsData, ListApprovalsErrors, ListApprovalsResponses, ListAssessmentsData, ListAssessmentsErrors, ListAssessmentsResponses, ListBundleData, ListBundleErrors, ListBundleResponses, ListConversationArchiveData, ListConversationArchiveErrors, ListConversationArchiveResponses, ListConversationDatasetsData, ListConversationDatasetsErrors, ListConversationDatasetsResponses, ListConversationExportsData, ListConversationExportsErrors, ListConversationExportsResponses, ListConversationsData, ListConversationsResponses, ListConversationTurnsData, ListConversationTurnsErrors, ListConversationTurnsResponses, ListDatasetsData, ListDatasetsErrors, ListDatasetsResponses, ListDimensionData, ListDimensionErrors, ListDimensionResponses, ListEvaluationsData, ListEvaluationsResponses, ListEvaluationSuitesData, ListEvaluationSuitesResponses, ListExperimentsData, ListExperimentsErrors, ListExperimentsResponses, ListExportsData, ListExportsErrors, ListExportsResponses, ListHubRowsData, ListHubRowsErrors, ListHubRowsResponses, ListHubsData, ListHubsErrors, ListHubsResponses, ListImagesData, ListImagesErrors, ListImagesResponses, ListImportBatchesData, ListImportBatchesErrors, ListImportBatchesResponses, ListImportJobsData, ListImportJobsErrors, ListImportJobsResponses, ListImportManifestsData, ListImportManifestsErrors, ListImportManifestsResponses, ListImportRejectsData, ListImportRejectsErrors, ListImportRejectsResponses, ListLabsData, ListLabsErrors, ListLabsResponses, ListLinesData, ListLinesErrors, ListLinesResponses, ListModelsData, ListModelsErrors, ListModelsResponses, ListPipelinesData, ListPipelinesErrors, ListPipelinesResponses, ListProjectsData, ListProjectsErrors, ListProjectsResponses, ListPromptsData, ListPromptsErrors, ListPromptsResponses, ListRecipesData, ListRecipesErrors, ListRecipesResponses, ListResultsData, ListResultsResponses, ListReviewsData, ListReviewsErrors, ListReviewsResponses, ListRubricsData, ListRubricsErrors, ListRubricsResponses, ListRunsData, ListRunsResponses, ListScorecardsData, ListScorecardsErrors, ListScorecardsResponses, ListScorecardVersionsData, ListScorecardVersionsErrors, ListScorecardVersionsResponses, ListSourcesData, ListSourcesResponses, ListSpansData, ListSpansResponses, ListTrainingRunsData, ListTrainingRunsErrors, ListTrainingRunsResponses, ListWorkflowDefinitionsData, ListWorkflowDefinitionsErrors, ListWorkflowDefinitionsResponses, ListWorkflowExecutionsData, ListWorkflowExecutionsResponses, ListWorkflowsData, ListWorkflowsResponses, LiveWebsocketData, LivezData, LivezResponses, LoginData, LoginErrors, LogoutData, LogoutResponses, MeData, MeErrors, MeResponses, OpenEditorData, OpenEditorErrors, OpenEditorResponses, OrganizationsData, OrganizationsErrors, OrganizationsResponses, PauseExecutionData, PauseExecutionErrors, PauseExecutionResponses, ProjectApproveSourceData, ProjectApproveSourceErrors, ProjectApproveSourceResponses, ProjectBuildExportData, ProjectBuildExportErrors, ProjectBuildExportResponses, ProjectCompareCasesData, ProjectCompareCasesErrors, ProjectCompareCasesResponses, ProjectCompareResultsData, ProjectCompareResultsErrors, ProjectCompareResultsResponses, ProjectDeclareScoringRunData, ProjectDeclareScoringRunErrors, ProjectDeclareScoringRunResponses, ProjectDeriveCohortData, ProjectDeriveCohortErrors, ProjectDeriveCohortResponses, ProjectDiffScorecardData, ProjectDiffScorecardErrors, ProjectDiffScorecardResponses, ProjectDiscardBundleData, ProjectDiscardBundleErrors, ProjectDiscardBundleResponses, ProjectFinishTrainingRunData, ProjectFinishTrainingRunErrors, ProjectFinishTrainingRunResponses, ProjectForgetResultData, ProjectForgetResultErrors, ProjectForgetResultResponses, ProjectGetAssessmentHistoryData, ProjectGetAssessmentHistoryErrors, ProjectGetAssessmentHistoryResponses, ProjectGetBlobData, ProjectGetBlobErrors, ProjectGetBlobResponses, ProjectGetCalibrationData, ProjectGetCalibrationErrors, ProjectGetCalibrationResponses, ProjectGetCasesData, ProjectGetCasesErrors, ProjectGetCasesResponses, ProjectGetDatasetRowsData, ProjectGetDatasetRowsErrors, ProjectGetDatasetRowsResponses, ProjectGetDerivedCohortData, ProjectGetDerivedCohortErrors, ProjectGetDerivedCohortResponses, ProjectGetEvaluationData, ProjectGetEvaluationErrors, ProjectGetEvaluationResponses, ProjectGetExperimentData, ProjectGetExperimentErrors, ProjectGetExperimentResponses, ProjectGetExportCocoData, ProjectGetExportCocoErrors, ProjectGetExportCocoResponses, ProjectGetExportData, ProjectGetExportErrors, ProjectGetExportResponses, ProjectGetImageData, ProjectGetImageErrors, ProjectGetImageResponses, ProjectGetLabData, ProjectGetLabErrors, ProjectGetLabMeasurementData, ProjectGetLabMeasurementErrors, ProjectGetLabMeasurementResponses, ProjectGetLabResponses, ProjectGetLabVersionData, ProjectGetLabVersionErrors, ProjectGetLabVersionResponses, ProjectGetMetricsData, ProjectGetMetricsErrors, ProjectGetMetricsResponses, ProjectGetModelData, ProjectGetModelErrors, ProjectGetModelResponses, ProjectGetOptimizationData, ProjectGetOptimizationErrors, ProjectGetOptimizationResponses, ProjectGetPipelineRevisionData, ProjectGetPipelineRevisionErrors, ProjectGetPipelineRevisionResponses, ProjectGetProjectData, ProjectGetProjectErrors, ProjectGetProjectResponses, ProjectGetPromptData, ProjectGetPromptErrors, ProjectGetPromptResponses, ProjectGetPromptVersionData, ProjectGetPromptVersionErrors, ProjectGetPromptVersionResponses, ProjectGetRecordingData, ProjectGetRecordingErrors, ProjectGetRecordingResponses, ProjectGetResultData, ProjectGetResultErrors, ProjectGetResultResponses, ProjectGetRubricData, ProjectGetRubricErrors, ProjectGetRubricResponses, ProjectGetRunData, ProjectGetRunErrors, ProjectGetRunEventsData, ProjectGetRunEventsErrors, ProjectGetRunEventsResponses, ProjectGetRunResponses, ProjectGetScorecardData, ProjectGetScorecardErrors, ProjectGetScorecardResponses, ProjectGetScoringRunData, ProjectGetScoringRunErrors, ProjectGetScoringRunResponses, ProjectGetTrainingRunData, ProjectGetTrainingRunErrors, ProjectGetTrainingRunResponses, ProjectGetWorkflowData, ProjectGetWorkflowDefinitionData, ProjectGetWorkflowDefinitionErrors, ProjectGetWorkflowDefinitionResponses, ProjectGetWorkflowErrors, ProjectGetWorkflowExecutionData, ProjectGetWorkflowExecutionErrors, ProjectGetWorkflowExecutionResponses, ProjectGetWorkflowResponses, ProjectGrantsData, ProjectGrantsErrors, ProjectGrantsResponses, ProjectListApprovalsData, ProjectListApprovalsErrors, ProjectListApprovalsResponses, ProjectListAssessmentsData, ProjectListAssessmentsErrors, ProjectListAssessmentsResponses, ProjectListBundleData, ProjectListBundleErrors, ProjectListBundleResponses, ProjectListConversationsData, ProjectListConversationsErrors, ProjectListConversationsResponses, ProjectListDatasetsData, ProjectListDatasetsErrors, ProjectListDatasetsResponses, ProjectListDimensionData, ProjectListDimensionErrors, ProjectListDimensionResponses, ProjectListEvaluationsData, ProjectListEvaluationsErrors, ProjectListEvaluationsResponses, ProjectListEvaluationSuitesData, ProjectListEvaluationSuitesErrors, ProjectListEvaluationSuitesResponses, ProjectListExperimentsData, ProjectListExperimentsErrors, ProjectListExperimentsResponses, ProjectListExportsData, ProjectListExportsErrors, ProjectListExportsResponses, ProjectListImagesData, ProjectListImagesErrors, ProjectListImagesResponses, ProjectListLabsData, ProjectListLabsErrors, ProjectListLabsResponses, ProjectListModelsData, ProjectListModelsErrors, ProjectListModelsResponses, ProjectListPipelinesData, ProjectListPipelinesErrors, ProjectListPipelinesResponses, ProjectListProjectsData, ProjectListProjectsErrors, ProjectListProjectsResponses, ProjectListPromptsData, ProjectListPromptsErrors, ProjectListPromptsResponses, ProjectListRecipesData, ProjectListRecipesErrors, ProjectListRecipesResponses, ProjectListResultsData, ProjectListResultsErrors, ProjectListResultsResponses, ProjectListReviewsData, ProjectListReviewsErrors, ProjectListReviewsResponses, ProjectListRubricsData, ProjectListRubricsErrors, ProjectListRubricsResponses, ProjectListRunsData, ProjectListRunsErrors, ProjectListRunsResponses, ProjectListScorecardsData, ProjectListScorecardsErrors, ProjectListScorecardsResponses, ProjectListScorecardVersionsData, ProjectListScorecardVersionsErrors, ProjectListScorecardVersionsResponses, ProjectListSpansData, ProjectListSpansErrors, ProjectListSpansResponses, ProjectListTrainingRunsData, ProjectListTrainingRunsErrors, ProjectListTrainingRunsResponses, ProjectListWorkflowDefinitionsData, ProjectListWorkflowDefinitionsErrors, ProjectListWorkflowDefinitionsResponses, ProjectListWorkflowExecutionsData, ProjectListWorkflowExecutionsErrors, ProjectListWorkflowExecutionsResponses, ProjectListWorkflowsData, ProjectListWorkflowsErrors, ProjectListWorkflowsResponses, ProjectLiveWebsocketData, ProjectLiveWebsocketErrors, ProjectProposeCaseData, ProjectProposeCaseErrors, ProjectProposeCaseResponses, ProjectPublishDatasetData, ProjectPublishDatasetErrors, ProjectPublishDatasetResponses, ProjectPublishDatasetSampleData, ProjectPublishDatasetSampleErrors, ProjectPublishDatasetSampleResponses, ProjectPublishLabData, ProjectPublishLabErrors, ProjectPublishLabResponses, ProjectPublishPromptData, ProjectPublishPromptErrors, ProjectPublishPromptResponses, ProjectPublishResultData, ProjectPublishResultErrors, ProjectPublishResultResponses, ProjectPublishReviewsData, ProjectPublishReviewsErrors, ProjectPublishReviewsResponses, ProjectPublishRubricData, ProjectPublishRubricErrors, ProjectPublishRubricResponses, ProjectPublishScorecardData, ProjectPublishScorecardErrors, ProjectPublishScorecardResponses, ProjectRebuildPromptData, ProjectRebuildPromptErrors, ProjectRebuildPromptResponses, ProjectRecordAssessmentData, ProjectRecordAssessmentErrors, ProjectRecordAssessmentResponses, ProjectRecordOptimizationData, ProjectRecordOptimizationErrors, ProjectRecordOptimizationResponses, ProjectRecordTrainingProgressData, ProjectRecordTrainingProgressErrors, ProjectRecordTrainingProgressResponses, ProjectRegisterImageData, ProjectRegisterImageErrors, ProjectRegisterImageResponses, ProjectRegisterModelData, ProjectRegisterModelErrors, ProjectRegisterModelResponses, ProjectRegisterWorkflowData, ProjectRegisterWorkflowErrors, ProjectRegisterWorkflowResponses, ProjectReviewCaseData, ProjectReviewCaseErrors, ProjectReviewCaseResponses, ProjectReviewImageData, ProjectReviewImageErrors, ProjectReviewImageResponses, ProjectReviewsOfTargetData, ProjectReviewsOfTargetErrors, ProjectReviewsOfTargetResponses, ProjectSaveBlockTemplateData, ProjectSaveBlockTemplateErrors, ProjectSaveBlockTemplateResponses, ProjectSavePipelineData, ProjectSavePipelineErrors, ProjectSavePipelineResponses, ProjectSaveProjectData, ProjectSaveProjectErrors, ProjectSaveProjectResponses, ProjectSaveRecipeData, ProjectSaveRecipeErrors, ProjectSaveRecipeResponses, ProjectSaveRevisionData, ProjectSaveRevisionErrors, ProjectSaveRevisionResponses, ProjectsData, ProjectSearchBlockLibraryData, ProjectSearchBlockLibraryErrors, ProjectSearchBlockLibraryResponses, ProjectsErrors, ProjectSetLabLabelData, ProjectSetLabLabelErrors, ProjectSetLabLabelResponses, ProjectSetModelLabelData, ProjectSetModelLabelErrors, ProjectSetModelLabelResponses, ProjectSetPromptLabelData, ProjectSetPromptLabelErrors, ProjectSetPromptLabelResponses, ProjectsResponses, ProjectStageBundleData, ProjectStageBundleErrors, ProjectStageBundleResponses, ProjectStageRecordingData, ProjectStageRecordingErrors, ProjectStageRecordingResponses, ProjectStartScoringRunData, ProjectStartScoringRunErrors, ProjectStartScoringRunResponses, ProjectStartTrainingRunData, ProjectStartTrainingRunErrors, ProjectStartTrainingRunResponses, ProjectStreamEventsData, ProjectStreamEventsErrors, ProjectStreamEventsResponses, ProjectStreamRunData, ProjectStreamRunErrors, ProjectStreamRunResponses, ProjectStreamWorkflowExecutionData, ProjectStreamWorkflowExecutionErrors, ProjectStreamWorkflowExecutionResponses, ProjectTakeCalibrationData, ProjectTakeCalibrationErrors, ProjectTakeCalibrationResponses, ProjectUploadBlobData, ProjectUploadBlobErrors, ProjectUploadBlobResponses, ProjectWithdrawApprovalData, ProjectWithdrawApprovalErrors, ProjectWithdrawApprovalResponses, ProposeCaseData, ProposeCaseErrors, ProposeCaseResponses, ProvideInputData, ProvideInputErrors, ProvideInputResponses, PublishAlertRuleData, PublishAlertRuleErrors, PublishAlertRuleResponses, PublishDatasetData, PublishDatasetErrors, PublishDatasetResponses, PublishDatasetSampleData, PublishDatasetSampleErrors, PublishDatasetSampleResponses, PublishLabData, PublishLabErrors, PublishLabResponses, PublishPromptData, PublishPromptErrors, PublishPromptResponses, PublishResultData, PublishResultErrors, PublishResultResponses, PublishReviewsData, PublishReviewsErrors, PublishReviewsResponses, PublishRubricData, PublishRubricErrors, PublishRubricResponses, PublishScorecardData, PublishScorecardErrors, PublishScorecardResponses, QueueImportJobData, QueueImportJobErrors, QueueImportJobResponses, ReadDeciderLeaseData, ReadDeciderLeaseErrors, ReadDeciderLeaseResponses, ReadInputData, ReadInputErrors, ReadInputResponses, ReadPayloadData, ReadPayloadErrors, ReadPayloadResponses, ReadyzData, ReadyzErrors, ReadyzResponses, RebuildPromptData, RebuildPromptErrors, RebuildPromptResponses, RecordAssessmentData, RecordAssessmentErrors, RecordAssessmentResponses, RecordConversationTurnsData, RecordConversationTurnsErrors, RecordConversationTurnsResponses, RecordOptimizationData, RecordOptimizationErrors, RecordOptimizationResponses, RecordTrainingProgressData, RecordTrainingProgressErrors, RecordTrainingProgressResponses, RedeemData, RedeemErrors, RedeemResponses, RegisterImageData, RegisterImageErrors, RegisterImageResponses, RegisterModelData, RegisterModelErrors, RegisterModelResponses, RegisterWorkflowData, RegisterWorkflowErrors, RegisterWorkflowResponses, ReleaseDeciderLeaseData, ReleaseDeciderLeaseErrors, ReleaseDeciderLeaseResponses, ReportData, ReportErrors, ReportResponses, RerunWorkflowData, RerunWorkflowErrors, RerunWorkflowResponses, ResumeExecutionData, ResumeExecutionErrors, ResumeExecutionResponses, RetryAlertDeliveryData, RetryAlertDeliveryErrors, RetryAlertDeliveryResponses, RetryStepData, RetryStepErrors, RetryStepResponses, ReviewCaseData, ReviewCaseErrors, ReviewCaseResponses, ReviewConversationTurnData, ReviewConversationTurnErrors, ReviewConversationTurnResponses, ReviewImageData, ReviewImageErrors, ReviewImageResponses, ReviewsOfTargetData, ReviewsOfTargetErrors, ReviewsOfTargetResponses, RevokeInvitationData, RevokeInvitationErrors, RevokeInvitationResponses, RosterData, RosterErrors, RosterResponses, RunArtifactsData, RunArtifactsErrors, RunArtifactsResponses, RunBlocksData, RunBlocksErrors, RunBlocksResponses, SaveBlockTemplateData, SaveBlockTemplateErrors, SaveBlockTemplateResponses, SavePipelineData, SavePipelineErrors, SavePipelineResponses, SaveProjectData, SaveProjectErrors, SaveProjectResponses, SaveRecipeData, SaveRecipeErrors, SaveRecipeResponses, SaveRevisionData, SaveRevisionErrors, SaveRevisionResponses, SealPayloadData, SealPayloadErrors, SealPayloadResponses, SearchBlockLibraryData, SearchBlockLibraryErrors, SearchBlockLibraryResponses, SearchHubsData, SearchHubsErrors, SearchHubsResponses, SetAlertRuleEnabledData, SetAlertRuleEnabledErrors, SetAlertRuleEnabledResponses, SetLabLabelData, SetLabLabelErrors, SetLabLabelResponses, SetModelLabelData, SetModelLabelErrors, SetModelLabelResponses, SetPromptLabelData, SetPromptLabelErrors, SetPromptLabelResponses, SetScheduleData, SetScheduleErrors, SetScheduleResponses, SetWorkflowScheduleData, SetWorkflowScheduleErrors, SetWorkflowScheduleResponses, StageBundleData, StageBundleErrors, StageBundleResponses, StageImportBatchData, StageImportBatchErrors, StageImportBatchResponses, StageRecordingData, StageRecordingErrors, StageRecordingResponses, StageVariantArtifactData, StageVariantArtifactErrors, StageVariantArtifactResponses, StartExecutionData, StartExecutionErrors, StartExecutionResponses, StartScoringRunData, StartScoringRunErrors, StartScoringRunResponses, StartTrainingRunData, StartTrainingRunErrors, StartTrainingRunResponses, StepContextData, StepContextErrors, StepContextResponses, StreamEventsData, StreamEventsResponses, StreamRunData, StreamRunResponses, StreamWorkflowExecutionData, StreamWorkflowExecutionResponses, SystemData, SystemErrors, SystemResponses, TakeCalibrationData, TakeCalibrationErrors, TakeCalibrationResponses, TakeDeciderLeaseData, TakeDeciderLeaseErrors, TakeDeciderLeaseResponses, TestAlertChannelData, TestAlertChannelErrors, TestAlertChannelResponses, UploadBlobData, UploadBlobErrors, UploadBlobResponses, WithdrawApprovalData, WithdrawApprovalErrors, WithdrawApprovalResponses, WithdrawLineData, WithdrawLineErrors, WithdrawLineResponses, WriteOutputData, WriteOutputErrors, WriteOutputResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * What this deployment sends through, and never where.
 */
export const getAlertChannel = <ThrowOnError extends boolean = false>(options?: Options<GetAlertChannelData, ThrowOnError>): RequestResult<GetAlertChannelResponses, GetAlertChannelErrors, ThrowOnError> => (options?.client ?? client).get<GetAlertChannelResponses, GetAlertChannelErrors, ThrowOnError>({ url: '/api/v1/alert-channel', ...options });

/**
 * Send one notification through the configured channel, now.
 *
 * Synchronous on purpose. A test that queued something would answer
 * "accepted" for a webhook nobody is listening on, which is the one answer a
 * test must not give. It leaves a record in the history like any other
 * delivery, so what a receiver saw can be found afterwards.
 */
export const testAlertChannel = <ThrowOnError extends boolean = false>(options?: Options<TestAlertChannelData, ThrowOnError>): RequestResult<TestAlertChannelResponses, TestAlertChannelErrors, ThrowOnError> => (options?.client ?? client).post<TestAlertChannelResponses, TestAlertChannelErrors, ThrowOnError>({ url: '/api/v1/alert-channel/test', ...options });

/**
 * What was sent, newest first.
 */
export const listAlertDeliveries = <ThrowOnError extends boolean = false>(options?: Options<ListAlertDeliveriesData, ThrowOnError>): RequestResult<ListAlertDeliveriesResponses, ListAlertDeliveriesErrors, ThrowOnError> => (options?.client ?? client).get<ListAlertDeliveriesResponses, ListAlertDeliveriesErrors, ThrowOnError>({ url: '/api/v1/alert-deliveries', ...options });

/**
 * One delivery, by its dedup key.
 */
export const getAlertDelivery = <ThrowOnError extends boolean = false>(options: Options<GetAlertDeliveryData, ThrowOnError>): RequestResult<GetAlertDeliveryResponses, GetAlertDeliveryErrors, ThrowOnError> => (options.client ?? client).get<GetAlertDeliveryResponses, GetAlertDeliveryErrors, ThrowOnError>({ url: '/api/v1/alert-deliveries/{key}', ...options });

/**
 * Send a failed delivery again.
 *
 * The budget starts over, which is the point: a channel that was down for an
 * hour outlives three attempts, and somebody deciding this one is still worth
 * sending is a different fact from a receiver that refused the body. A
 * delivery that did not fail answers 409 — re-sending a delivered one would
 * be this system creating the duplicate it exists to avoid.
 */
export const retryAlertDelivery = <ThrowOnError extends boolean = false>(options: Options<RetryAlertDeliveryData, ThrowOnError>): RequestResult<RetryAlertDeliveryResponses, RetryAlertDeliveryErrors, ThrowOnError> => (options.client ?? client).post<RetryAlertDeliveryResponses, RetryAlertDeliveryErrors, ThrowOnError>({ url: '/api/v1/alert-deliveries/{key}/retry', ...options });

/**
 * Every alert rule, by name.
 */
export const listAlertRules = <ThrowOnError extends boolean = false>(options?: Options<ListAlertRulesData, ThrowOnError>): RequestResult<ListAlertRulesResponses, ListAlertRulesErrors, ThrowOnError> => (options?.client ?? client).get<ListAlertRulesResponses, ListAlertRulesErrors, ThrowOnError>({ url: '/api/v1/alert-rules', ...options });

/**
 * Publish a version of an alert rule.
 *
 * Idempotent on the document: `created` is `false` when this exact rule was
 * already stored, and nothing that already fired under it fires again.
 */
export const publishAlertRule = <ThrowOnError extends boolean = false>(options: Options<PublishAlertRuleData, ThrowOnError>): RequestResult<PublishAlertRuleResponses, PublishAlertRuleErrors, ThrowOnError> => (options.client ?? client).post<PublishAlertRuleResponses, PublishAlertRuleErrors, ThrowOnError>({
    url: '/api/v1/alert-rules',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One rule: its versions, and the one it is at now.
 */
export const getAlertRule = <ThrowOnError extends boolean = false>(options: Options<GetAlertRuleData, ThrowOnError>): RequestResult<GetAlertRuleResponses, GetAlertRuleErrors, ThrowOnError> => (options.client ?? client).get<GetAlertRuleResponses, GetAlertRuleErrors, ThrowOnError>({ url: '/api/v1/alert-rules/{name}', ...options });

/**
 * Switch a rule on or off.
 *
 * Not an edit: no version is published, the dedup key does not move, and
 * switching a rule back on does not re-send what it already sent while it was
 * on. Silencing a rule during an incident is what this is for.
 */
export const setAlertRuleEnabled = <ThrowOnError extends boolean = false>(options: Options<SetAlertRuleEnabledData, ThrowOnError>): RequestResult<SetAlertRuleEnabledResponses, SetAlertRuleEnabledErrors, ThrowOnError> => (options.client ?? client).put<SetAlertRuleEnabledResponses, SetAlertRuleEnabledErrors, ThrowOnError>({
    url: '/api/v1/alert-rules/{name}/enabled',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One version of a rule.
 */
export const getAlertRuleVersion = <ThrowOnError extends boolean = false>(options: Options<GetAlertRuleVersionData, ThrowOnError>): RequestResult<GetAlertRuleVersionResponses, GetAlertRuleVersionErrors, ThrowOnError> => (options.client ?? client).get<GetAlertRuleVersionResponses, GetAlertRuleVersionErrors, ThrowOnError>({ url: '/api/v1/alert-rules/{name}/versions/{version_id}', ...options });

/**
 * Store an image under the digest of its own bytes.
 *
 * The digest is computed here. A content address supplied by the caller is
 * not a content address, and trusting one would let two different images
 * occupy the same key.
 */
export const uploadBlob = <ThrowOnError extends boolean = false>(options: Options<UploadBlobData, ThrowOnError>): RequestResult<UploadBlobResponses, UploadBlobErrors, ThrowOnError> => (options.client ?? client).post<UploadBlobResponses, UploadBlobErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/annotation-blobs',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * The bytes of an uploaded image.
 *
 * Immutable by construction — the key *is* the digest — so it is cached for a
 * year on legacy routes. Project routes override this with no-store so
 * revoked access is checked on the next read. An annotation canvas re-fetching a 2 MB plan on every pan would be the
 * slowest part of the tool.
 */
export const getBlob = <ThrowOnError extends boolean = false>(options: Options<GetBlobData, ThrowOnError>): RequestResult<GetBlobResponses, GetBlobErrors, ThrowOnError> => (options.client ?? client).get<GetBlobResponses, GetBlobErrors, ThrowOnError>({ url: '/api/v1/annotation-blobs/{image_id}', ...options });

/**
 * One immutable manifest: its samples, its splits, and every exclusion with
 * its reason.
 */
export const getExport = <ThrowOnError extends boolean = false>(options: Options<GetExportData, ThrowOnError>): RequestResult<GetExportResponses, GetExportErrors, ThrowOnError> => (options.client ?? client).get<GetExportResponses, GetExportErrors, ThrowOnError>({ url: '/api/v1/annotation-export', ...options });

/**
 * The same export as a COCO document.
 *
 * Generated rather than stored: COCO is a derived view of the vector
 * annotations, and storing it would be a second copy of the truth that can
 * disagree with the first. It reads one revision object per sample, which is
 * paid once per training run rather than once per epoch.
 */
export const getExportCoco = <ThrowOnError extends boolean = false>(options: Options<GetExportCocoData, ThrowOnError>): RequestResult<GetExportCocoResponses, GetExportCocoErrors, ThrowOnError> => (options.client ?? client).get<GetExportCocoResponses, GetExportCocoErrors, ThrowOnError>({ url: '/api/v1/annotation-export/coco', ...options });

/**
 * Every export built from a project, newest first.
 */
export const listExports = <ThrowOnError extends boolean = false>(options: Options<ListExportsData, ThrowOnError>): RequestResult<ListExportsResponses, ListExportsErrors, ThrowOnError> => (options.client ?? client).get<ListExportsResponses, ListExportsErrors, ThrowOnError>({ url: '/api/v1/annotation-exports', ...options });

/**
 * Freeze the project as it stands into an immutable, content-addressed
 * manifest.
 *
 * The reference a training run records is `project@export`. Two exports of an
 * unchanged project are one export, which is why re-running this nightly is
 * cheap and why a manifest is a comparison boundary rather than a timestamp.
 */
export const buildExport = <ThrowOnError extends boolean = false>(options: Options<BuildExportData, ThrowOnError>): RequestResult<BuildExportResponses, BuildExportErrors, ThrowOnError> => (options.client ?? client).post<BuildExportResponses, BuildExportErrors, ThrowOnError>({
    url: '/api/v1/annotation-exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One image, its revision history, one revision's shapes, and the side of the
 * split its family is on.
 */
export const getImage = <ThrowOnError extends boolean = false>(options: Options<GetImageData, ThrowOnError>): RequestResult<GetImageResponses, GetImageErrors, ThrowOnError> => (options.client ?? client).get<GetImageResponses, GetImageErrors, ThrowOnError>({ url: '/api/v1/annotation-image', ...options });

/**
 * The images in a project, newest registration first.
 */
export const listImages = <ThrowOnError extends boolean = false>(options: Options<ListImagesData, ThrowOnError>): RequestResult<ListImagesResponses, ListImagesErrors, ThrowOnError> => (options.client ?? client).get<ListImagesResponses, ListImagesErrors, ThrowOnError>({ url: '/api/v1/annotation-images', ...options });

/**
 * Register an image into a project, or correct what is known about it.
 *
 * Re-registering keeps the revisions and the review state: a plan re-submitted
 * with a corrected `group_id` is a correction, and resetting its labels would
 * punish the correction.
 */
export const registerImage = <ThrowOnError extends boolean = false>(options: Options<RegisterImageData, ThrowOnError>): RequestResult<RegisterImageResponses, RegisterImageErrors, ThrowOnError> => (options.client ?? client).post<RegisterImageResponses, RegisterImageErrors, ThrowOnError>({
    url: '/api/v1/annotation-images',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const getImportBatch = <ThrowOnError extends boolean = false>(options: Options<GetImportBatchData, ThrowOnError>): RequestResult<GetImportBatchResponses, GetImportBatchErrors, ThrowOnError> => (options.client ?? client).get<GetImportBatchResponses, GetImportBatchErrors, ThrowOnError>({ url: '/api/v1/annotation-import-batch', ...options });

export const listImportBatches = <ThrowOnError extends boolean = false>(options?: Options<ListImportBatchesData, ThrowOnError>): RequestResult<ListImportBatchesResponses, ListImportBatchesErrors, ThrowOnError> => (options?.client ?? client).get<ListImportBatchesResponses, ListImportBatchesErrors, ThrowOnError>({ url: '/api/v1/annotation-import-batches', ...options });

/**
 * Open a batch: what it is for, what may be done with it, and where it came
 * from.
 *
 * Rights, evidence and the Hub pin are decided here rather than per page,
 * because they are properties of the corpus. `revision` is the one worth
 * filling in even when it feels redundant: a dataset id is a moving target,
 * and an import that recorded only the name has recorded provenance nobody
 * can go back to.
 */
export const stageImportBatch = <ThrowOnError extends boolean = false>(options: Options<StageImportBatchData, ThrowOnError>): RequestResult<StageImportBatchResponses, StageImportBatchErrors, ThrowOnError> => (options.client ?? client).post<StageImportBatchResponses, StageImportBatchErrors, ThrowOnError>({
    url: '/api/v1/annotation-import-batches',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const getImportJob = <ThrowOnError extends boolean = false>(options: Options<GetImportJobData, ThrowOnError>): RequestResult<GetImportJobResponses, GetImportJobErrors, ThrowOnError> => (options.client ?? client).get<GetImportJobResponses, GetImportJobErrors, ThrowOnError>({ url: '/api/v1/annotation-import-job', ...options });

/**
 * Stop a job. What it already registered stays registered.
 *
 * Images are content-addressed, so nothing is half-written; what a cancelled
 * job does not get is a manifest, which is what stops it appearing as a
 * completed import.
 */
export const cancelImportJob = <ThrowOnError extends boolean = false>(options: Options<CancelImportJobData, ThrowOnError>): RequestResult<CancelImportJobResponses, CancelImportJobErrors, ThrowOnError> => (options.client ?? client).post<CancelImportJobResponses, CancelImportJobErrors, ThrowOnError>({ url: '/api/v1/annotation-import-job/cancel', ...options });

export const listImportJobs = <ThrowOnError extends boolean = false>(options?: Options<ListImportJobsData, ThrowOnError>): RequestResult<ListImportJobsResponses, ListImportJobsErrors, ThrowOnError> => (options?.client ?? client).get<ListImportJobsResponses, ListImportJobsErrors, ThrowOnError>({ url: '/api/v1/annotation-import-jobs', ...options });

/**
 * Seal the batch and queue the job that reads it.
 *
 * A 200 rather than a 201: what comes back is a job, and the thing it will
 * produce does not exist yet. The rights check runs here, before anything is
 * registered — a claim contradicting what a human recorded about the corpus
 * is a decision to reverse, not a row to skip — so a 400 from this route is
 * about the corpus rather than about the request's shape.
 *
 * Always queue a `dry_run` first from a UI. Six hundred thousand images with
 * the split key mapped from a filename is not something to discover after the
 * fact, and a dry run costs the downloads and nothing else.
 */
export const queueImportJob = <ThrowOnError extends boolean = false>(options: Options<QueueImportJobData, ThrowOnError>): RequestResult<QueueImportJobResponses, QueueImportJobErrors, ThrowOnError> => (options.client ?? client).post<QueueImportJobResponses, QueueImportJobErrors, ThrowOnError>({
    url: '/api/v1/annotation-import-jobs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One import's receipt: what it read, what it registered, what it refused,
 * and on what terms.
 */
export const getImportManifest = <ThrowOnError extends boolean = false>(options: Options<GetImportManifestData, ThrowOnError>): RequestResult<GetImportManifestResponses, GetImportManifestErrors, ThrowOnError> => (options.client ?? client).get<GetImportManifestResponses, GetImportManifestErrors, ThrowOnError>({ url: '/api/v1/annotation-import-manifest', ...options });

/**
 * Every published import, newest first.
 */
export const listImportManifests = <ThrowOnError extends boolean = false>(options?: Options<ListImportManifestsData, ThrowOnError>): RequestResult<ListImportManifestsResponses, ListImportManifestsErrors, ThrowOnError> => (options?.client ?? client).get<ListImportManifestsResponses, ListImportManifestsErrors, ThrowOnError>({ url: '/api/v1/annotation-import-manifests', ...options });

/**
 * The rows a job refused, and why.
 *
 * The dead-letter half, and the reason it is a route rather than a field on
 * the job: an import that rejected four hundred thousand rows cannot put them
 * in a response, and "read the counts, then page the rows" is the difference
 * between a diagnosable import and a number.
 */
export const listImportRejects = <ThrowOnError extends boolean = false>(options: Options<ListImportRejectsData, ThrowOnError>): RequestResult<ListImportRejectsResponses, ListImportRejectsErrors, ThrowOnError> => (options.client ?? client).get<ListImportRejectsResponses, ListImportRejectsErrors, ThrowOnError>({ url: '/api/v1/annotation-import-rejects', ...options });

/**
 * Add one page of rows to an open batch.
 *
 * Number the page when the client can. A numbered append is idempotent —
 * identical bytes are an acknowledged retry, different bytes for a page
 * already stored are a 400 naming it — which is what makes a million rows
 * over a flaky link a thing that can be re-sent rather than reconciled.
 */
export const appendImportRows = <ThrowOnError extends boolean = false>(options: Options<AppendImportRowsData, ThrowOnError>): RequestResult<AppendImportRowsResponses, AppendImportRowsErrors, ThrowOnError> => (options.client ?? client).post<AppendImportRowsResponses, AppendImportRowsErrors, ThrowOnError>({
    url: '/api/v1/annotation-import-rows',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Register many images at once, from rows a Flow PHP pipeline produced.
 *
 * The write half of dataset discovery. A hub search says a corpus exists; a
 * Flow query maps its file listing onto this schema; this registers the
 * result into a project somebody can draw on.
 *
 * Two things about the response are worth reading rather than skipping. One
 * bad row does not fail the batch — `outcomes` names every refusal — and
 * `warnings` carries the states that *succeed* and still leave the corpus
 * worth less than it looks: unknown rights, and a group key that gave every
 * image its own family.
 *
 * Always `dry_run` first from a UI. Six hundred images with the split key
 * mapped from a filename is not something to discover after the fact.
 */
export const importImages = <ThrowOnError extends boolean = false>(options: Options<ImportImagesData, ThrowOnError>): RequestResult<ImportImagesResponses, ImportImagesErrors, ThrowOnError> => (options.client ?? client).post<ImportImagesResponses, ImportImagesErrors, ThrowOnError>({
    url: '/api/v1/annotation-imports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One project, with the counts that answer "is there enough data yet".
 */
export const getProject = <ThrowOnError extends boolean = false>(options: Options<GetProjectData, ThrowOnError>): RequestResult<GetProjectResponses, GetProjectErrors, ThrowOnError> => (options.client ?? client).get<GetProjectResponses, GetProjectErrors, ThrowOnError>({ url: '/api/v1/annotation-project', ...options });

/**
 * Every annotation project.
 */
export const listProjects = <ThrowOnError extends boolean = false>(options?: Options<ListProjectsData, ThrowOnError>): RequestResult<ListProjectsResponses, ListProjectsErrors, ThrowOnError> => (options?.client ?? client).get<ListProjectsResponses, ListProjectsErrors, ThrowOnError>({ url: '/api/v1/annotation-projects', ...options });

/**
 * Create a project, or replace its description, split policy and label schema.
 *
 * The schema version is re-derived from the classes. An unchanged class list
 * keeps its version and every accepted revision stays valid; a changed one is
 * a new version, and the next export names every revision drawn under the old
 * one as excluded rather than quietly relabelling them.
 */
export const saveProject = <ThrowOnError extends boolean = false>(options: Options<SaveProjectData, ThrowOnError>): RequestResult<SaveProjectResponses, SaveProjectErrors, ThrowOnError> => (options.client ?? client).post<SaveProjectResponses, SaveProjectErrors, ThrowOnError>({
    url: '/api/v1/annotation-projects',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Move an image's review state, and pin the revision an export will read.
 */
export const reviewImage = <ThrowOnError extends boolean = false>(options: Options<ReviewImageData, ThrowOnError>): RequestResult<ReviewImageResponses, ReviewImageErrors, ThrowOnError> => (options.client ?? client).post<ReviewImageResponses, ReviewImageErrors, ThrowOnError>({
    url: '/api/v1/annotation-reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Save a drawing.
 *
 * A revision that does not validate against the project's schema is not
 * stored at all, and the 422 carries every problem rather than the first.
 * Storing an invalid revision and marking it so would move the decision about
 * whether a half-drawn door is a training target to whatever reads it next.
 */
export const saveRevision = <ThrowOnError extends boolean = false>(options: Options<SaveRevisionData, ThrowOnError>): RequestResult<SaveRevisionResponses, SaveRevisionErrors, ThrowOnError> => (options.client ?? client).post<SaveRevisionResponses, SaveRevisionErrors, ThrowOnError>({
    url: '/api/v1/annotation-revisions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The public floor-plan corpora, and where to look for more.
 *
 * A dated table this build ships, not a search against Hugging Face or
 * Roboflow. Those mirrors restate licences wrongly often enough that a live
 * answer would be worse than no answer, because it would arrive looking
 * authoritative. Every row links its original.
 */
export const listSources = <ThrowOnError extends boolean = false>(options?: Options<ListSourcesData, ThrowOnError>): RequestResult<ListSourcesResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListSourcesResponses, unknown, ThrowOnError>({ url: '/api/v1/annotation-sources', ...options });

/**
 * Where the provider sends the browser back to.
 *
 * Every failure here ends in a redirect rather than a JSON error, because
 * this is a top-level navigation: whatever this returns is what the person
 * sees in their browser. The reason travels as `sign_in_error`, which the
 * panel reads and renders on its sign-in screen.
 */
export const callback = <ThrowOnError extends boolean = false>(options: Options<CallbackData, ThrowOnError>): RequestResult<unknown, unknown, ThrowOnError> => (options.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/v1/auth/callback', ...options });

/**
 * Whether this instance has a login, and what to call it.
 *
 * The one route besides the health probes that answers before anybody has
 * signed in. Without it the panel would have to guess, and guessing wrong
 * means either a sign-in screen on an instance with no provider or an endless
 * 401 loop on one that has.
 */
export const authConfig = <ThrowOnError extends boolean = false>(options?: Options<AuthConfigData, ThrowOnError>): RequestResult<AuthConfigResponses, unknown, ThrowOnError> => (options?.client ?? client).get<AuthConfigResponses, unknown, ThrowOnError>({ url: '/api/v1/auth/config', ...options });

/**
 * Start a sign-in: a redirect to the provider.
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>): RequestResult<unknown, LoginErrors, ThrowOnError> => (options.client ?? client).get<unknown, LoginErrors, ThrowOnError>({ url: '/api/v1/auth/login', ...options });

/**
 * Sign out.
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>): RequestResult<LogoutResponses, unknown, ThrowOnError> => (options?.client ?? client).post<LogoutResponses, unknown, ThrowOnError>({ url: '/api/v1/auth/logout', ...options });

/**
 * The current caller.
 */
export const me = <ThrowOnError extends boolean = false>(options?: Options<MeData, ThrowOnError>): RequestResult<MeResponses, MeErrors, ThrowOnError> => (options?.client ?? client).get<MeResponses, MeErrors, ThrowOnError>({ url: '/api/v1/auth/me', ...options });

/**
 * Every conversation the archive holds, newest activity first.
 */
export const listConversationArchive = <ThrowOnError extends boolean = false>(options?: Options<ListConversationArchiveData, ThrowOnError>): RequestResult<ListConversationArchiveResponses, ListConversationArchiveErrors, ThrowOnError> => (options?.client ?? client).get<ListConversationArchiveResponses, ListConversationArchiveErrors, ThrowOnError>({ url: '/api/v1/conversation-archive', ...options });

/**
 * One page of a corpus. Content, so `admin` — the same gate as reading a turn.
 *
 * A corpus an erasure has withdrawn answers 410, exactly as an erased turn
 * does: its manifest, counts and digests survive so a training run naming the
 * reference still resolves to something that can say what happened to it, and
 * only the rows are gone.
 */
export const getConversationDatasetRows = <ThrowOnError extends boolean = false>(options: Options<GetConversationDatasetRowsData, ThrowOnError>): RequestResult<GetConversationDatasetRowsResponses, GetConversationDatasetRowsErrors, ThrowOnError> => (options.client ?? client).get<GetConversationDatasetRowsResponses, GetConversationDatasetRowsErrors, ThrowOnError>({ url: '/api/v1/conversation-dataset-rows', ...options });

/**
 * Every immutable corpus this archive has produced.
 */
export const listConversationDatasets = <ThrowOnError extends boolean = false>(options?: Options<ListConversationDatasetsData, ThrowOnError>): RequestResult<ListConversationDatasetsResponses, ListConversationDatasetsErrors, ThrowOnError> => (options?.client ?? client).get<ListConversationDatasetsResponses, ListConversationDatasetsErrors, ThrowOnError>({ url: '/api/v1/conversation-datasets', ...options });

/**
 * Erase conversation content, wherever it has reached.
 *
 * Two steps, and the second is the one that is easy to forget: the archive's
 * content is removed, and then every published corpus that read one of these
 * conversations has its rows deleted too. Erasing the archive and leaving the
 * corpus would be an erasure in name only.
 *
 * What remains everywhere is the record: heads, digests, review decisions and
 * export manifests. That is what lets an auditor still be told what was there.
 */
export const eraseConversationContent = <ThrowOnError extends boolean = false>(options: Options<EraseConversationContentData, ThrowOnError>): RequestResult<EraseConversationContentResponses, EraseConversationContentErrors, ThrowOnError> => (options.client ?? client).post<EraseConversationContentResponses, EraseConversationContentErrors, ThrowOnError>({
    url: '/api/v1/conversation-erasures',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every export job, newest first.
 */
export const listConversationExports = <ThrowOnError extends boolean = false>(options?: Options<ListConversationExportsData, ThrowOnError>): RequestResult<ListConversationExportsResponses, ListConversationExportsErrors, ThrowOnError> => (options?.client ?? client).get<ListConversationExportsResponses, ListConversationExportsErrors, ThrowOnError>({ url: '/api/v1/conversation-exports', ...options });

/**
 * Queue an export of the archive into an immutable corpus.
 *
 * 202, not 201: what comes back is a job, and the corpus it will produce does
 * not exist yet. A 201 would be this API claiming a dataset that a worker has
 * not built.
 */
export const createConversationExport = <ThrowOnError extends boolean = false>(options: Options<CreateConversationExportData, ThrowOnError>): RequestResult<CreateConversationExportResponses, CreateConversationExportErrors, ThrowOnError> => (options.client ?? client).post<CreateConversationExportResponses, CreateConversationExportErrors, ThrowOnError>({
    url: '/api/v1/conversation-exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One job: where it is, what it has counted, and what it left out.
 */
export const getConversationExport = <ThrowOnError extends boolean = false>(options: Options<GetConversationExportData, ThrowOnError>): RequestResult<GetConversationExportResponses, GetConversationExportErrors, ThrowOnError> => (options.client ?? client).get<GetConversationExportResponses, GetConversationExportErrors, ThrowOnError>({ url: '/api/v1/conversation-exports/{job_id}', ...options });

/**
 * Stop a job. What it has written stays written; there is no version.
 */
export const cancelConversationExport = <ThrowOnError extends boolean = false>(options: Options<CancelConversationExportData, ThrowOnError>): RequestResult<CancelConversationExportResponses, CancelConversationExportErrors, ThrowOnError> => (options.client ?? client).post<CancelConversationExportResponses, CancelConversationExportErrors, ThrowOnError>({ url: '/api/v1/conversation-exports/{job_id}/cancel', ...options });

/**
 * What this instance demands before it will hold conversation content.
 */
export const conversationPolicy = <ThrowOnError extends boolean = false>(options?: Options<ConversationPolicyData, ThrowOnError>): RequestResult<ConversationPolicyResponses, ConversationPolicyErrors, ThrowOnError> => (options?.client ?? client).get<ConversationPolicyResponses, ConversationPolicyErrors, ThrowOnError>({ url: '/api/v1/conversation-policy', ...options });

/**
 * What was actually said. The one route that leaves the encryption boundary.
 *
 * A turn whose content has been erased answers 410 rather than 404: "it was
 * here and it is gone" is the answer an auditor came for, and a 404 would make
 * a completed erasure indistinguishable from a turn that never existed.
 */
export const conversationTurnContent = <ThrowOnError extends boolean = false>(options: Options<ConversationTurnContentData, ThrowOnError>): RequestResult<ConversationTurnContentResponses, ConversationTurnContentErrors, ThrowOnError> => (options.client ?? client).get<ConversationTurnContentResponses, ConversationTurnContentErrors, ThrowOnError>({ url: '/api/v1/conversation-turn-content', ...options });

/**
 * Approve or reject one turn, attributed to whoever is asking.
 */
export const reviewConversationTurn = <ThrowOnError extends boolean = false>(options: Options<ReviewConversationTurnData, ThrowOnError>): RequestResult<ReviewConversationTurnResponses, ReviewConversationTurnErrors, ThrowOnError> => (options.client ?? client).post<ReviewConversationTurnResponses, ReviewConversationTurnErrors, ThrowOnError>({
    url: '/api/v1/conversation-turn-reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One conversation's turns. Heads only: nothing here is decrypted.
 */
export const listConversationTurns = <ThrowOnError extends boolean = false>(options: Options<ListConversationTurnsData, ThrowOnError>): RequestResult<ListConversationTurnsResponses, ListConversationTurnsErrors, ThrowOnError> => (options.client ?? client).get<ListConversationTurnsResponses, ListConversationTurnsErrors, ThrowOnError>({ url: '/api/v1/conversation-turns', ...options });

/**
 * Record conversation content, with the consent and retention that permit it.
 */
export const recordConversationTurns = <ThrowOnError extends boolean = false>(options: Options<RecordConversationTurnsData, ThrowOnError>): RequestResult<RecordConversationTurnsResponses, RecordConversationTurnsErrors, ThrowOnError> => (options.client ?? client).post<RecordConversationTurnsResponses, RecordConversationTurnsErrors, ThrowOnError>({
    url: '/api/v1/conversation-turns',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Sessions, each grouping the runs it produced.
 *
 * The level above a run, so the panel can start from "which session" and walk
 * down to a single LLM call without going back to a flat list and re-filtering.
 */
export const listConversations = <ThrowOnError extends boolean = false>(options?: Options<ListConversationsData, ThrowOnError>): RequestResult<ListConversationsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListConversationsResponses, unknown, ThrowOnError>({ url: '/api/v1/conversations', ...options });

/**
 * Search reusable block solutions in the selected registry.
 */
export const searchBlockLibrary = <ThrowOnError extends boolean = false>(options?: Options<SearchBlockLibraryData, ThrowOnError>): RequestResult<SearchBlockLibraryResponses, SearchBlockLibraryErrors, ThrowOnError> => (options?.client ?? client).get<SearchBlockLibraryResponses, SearchBlockLibraryErrors, ThrowOnError>({ url: '/api/v1/curation-library', ...options });

/**
 * Publish a reusable solution through the same registry operation as the seed.
 */
export const saveBlockTemplate = <ThrowOnError extends boolean = false>(options: Options<SaveBlockTemplateData, ThrowOnError>): RequestResult<SaveBlockTemplateResponses, SaveBlockTemplateErrors, ThrowOnError> => (options.client ?? client).post<SaveBlockTemplateResponses, SaveBlockTemplateErrors, ThrowOnError>({
    url: '/api/v1/curation-library',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every saved curation pipeline, newest save first.
 */
export const listPipelines = <ThrowOnError extends boolean = false>(options?: Options<ListPipelinesData, ThrowOnError>): RequestResult<ListPipelinesResponses, ListPipelinesErrors, ThrowOnError> => (options?.client ?? client).get<ListPipelinesResponses, ListPipelinesErrors, ThrowOnError>({ url: '/api/v1/curation-pipelines', ...options });

/**
 * Save a content-addressed revision of a block pipeline.
 *
 * The blocks are checked as a chain here and nowhere else — the canvas draws
 * what it is told and implements no rules of its own, exactly as the
 * annotation canvas does not re-implement the shape validator. A refusal is a
 * 422 whose `details` carry every problem at once.
 */
export const savePipeline = <ThrowOnError extends boolean = false>(options: Options<SavePipelineData, ThrowOnError>): RequestResult<SavePipelineResponses, SavePipelineErrors, ThrowOnError> => (options.client ?? client).post<SavePipelineResponses, SavePipelineErrors, ThrowOnError>({
    url: '/api/v1/curation-pipelines',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read an exact saved pipeline, including its layout and notebook pins.
 * A missing revision never falls back to the current head.
 */
export const getPipelineRevision = <ThrowOnError extends boolean = false>(options: Options<GetPipelineRevisionData, ThrowOnError>): RequestResult<GetPipelineRevisionResponses, GetPipelineRevisionErrors, ThrowOnError> => (options.client ?? client).get<GetPipelineRevisionResponses, GetPipelineRevisionErrors, ThrowOnError>({ url: '/api/v1/curation-pipelines/{name}/revisions/{revision}', ...options });

/**
 * One authored block of one revision, as it would run.
 *
 * The revision is pinned in the path and never defaulted to the head: opening
 * a block of a saved pipeline means the pipeline as it was saved, and reading
 * the head instead would be the same answer for every revision.
 */
export const blockContext = <ThrowOnError extends boolean = false>(options: Options<BlockContextData, ThrowOnError>): RequestResult<BlockContextResponses, BlockContextErrors, ThrowOnError> => (options.client ?? client).get<BlockContextResponses, BlockContextErrors, ThrowOnError>({ url: '/api/v1/curation-pipelines/{name}/revisions/{revision}/blocks/{block_id}/context', ...options });

/**
 * Forget a schedule.
 *
 * Deleting and `enabled: false` are different things and both are offered:
 * one says "not any more", the other "not for now, and these are still the
 * settings".
 */
export const clearSchedule = <ThrowOnError extends boolean = false>(options: Options<ClearScheduleData, ThrowOnError>): RequestResult<ClearScheduleResponses, ClearScheduleErrors, ThrowOnError> => (options.client ?? client).delete<ClearScheduleResponses, ClearScheduleErrors, ThrowOnError>({ url: '/api/v1/curation-pipelines/{name}/schedule', ...options });

/**
 * One definition's schedule, or 404 when it has none.
 */
export const getSchedule = <ThrowOnError extends boolean = false>(options: Options<GetScheduleData, ThrowOnError>): RequestResult<GetScheduleResponses, GetScheduleErrors, ThrowOnError> => (options.client ?? client).get<GetScheduleResponses, GetScheduleErrors, ThrowOnError>({ url: '/api/v1/curation-pipelines/{name}/schedule', ...options });

/**
 * Set or replace a schedule, and optionally start it once.
 *
 * The definition is read first and a name nobody saved is a 404: a schedule
 * for a pipeline that does not exist is a run that fails every morning at nine
 * with nobody watching.
 */
export const setSchedule = <ThrowOnError extends boolean = false>(options: Options<SetScheduleData, ThrowOnError>): RequestResult<SetScheduleResponses, SetScheduleErrors, ThrowOnError> => (options.client ?? client).put<SetScheduleResponses, SetScheduleErrors, ThrowOnError>({
    url: '/api/v1/curation-pipelines/{name}/schedule',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every saved Flow PHP recipe, newest save first.
 */
export const listRecipes = <ThrowOnError extends boolean = false>(options?: Options<ListRecipesData, ThrowOnError>): RequestResult<ListRecipesResponses, ListRecipesErrors, ThrowOnError> => (options?.client ?? client).get<ListRecipesResponses, ListRecipesErrors, ThrowOnError>({ url: '/api/v1/curations', ...options });

/**
 * Save a content-addressed revision of a Flow PHP recipe.
 */
export const saveRecipe = <ThrowOnError extends boolean = false>(options: Options<SaveRecipeData, ThrowOnError>): RequestResult<SaveRecipeResponses, SaveRecipeErrors, ThrowOnError> => (options.client ?? client).post<SaveRecipeResponses, SaveRecipeErrors, ThrowOnError>({
    url: '/api/v1/curations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Which hubs are configured, and the variable to set for each that is not.
 */
export const listHubs = <ThrowOnError extends boolean = false>(options?: Options<ListHubsData, ThrowOnError>): RequestResult<ListHubsResponses, ListHubsErrors, ThrowOnError> => (options?.client ?? client).get<ListHubsResponses, ListHubsErrors, ThrowOnError>({ url: '/api/v1/dataset-hubs', ...options });

/**
 * The bytes of one picture inside a hub dataset.
 *
 * Exists because a hub does not always have an address for its own pictures.
 * A corpus that keeps them in a `binary` column hands over base64 in the row
 * and nothing else, so the row a query returns names the *cell* and this
 * resolves it — which is also what lets the panel draw such a picture at all,
 * same-origin and with no expiry.
 *
 * Not cached: it is a preview of somebody else's corpus, and the copy that is
 * meant to last is the blob an import writes.
 */
export const getHubImage = <ThrowOnError extends boolean = false>(options: Options<GetHubImageData, ThrowOnError>): RequestResult<GetHubImageResponses, GetHubImageErrors, ThrowOnError> => (options.client ?? client).get<GetHubImageResponses, GetHubImageErrors, ThrowOnError>({ url: '/api/v1/dataset-hubs/image', ...options });

/**
 * The rows inside one hub dataset.
 *
 * The other half of discovery, and a different question from the search: that
 * one answers "which corpora exist", this one "what is in this one". An
 * import built from a search result alone registers a single row pointing at
 * a web page — the corpus is the *rows*, and until this route existed there
 * was no way for a pipeline to read them.
 *
 * It reports the columns the corpus declares and hands the rows over as they
 * came. Which of them is a picture, what a caption is called and what a
 * family key is built from are decided in the script, because they are
 * questions about that corpus rather than about this one.
 *
 * A hub that cannot answer is a 502 carrying its sentence rather than an
 * empty list, for the same reason the search route's 501 is not one: "no
 * rows" is a claim about the corpus, and this is a claim about the request.
 */
export const listHubRows = <ThrowOnError extends boolean = false>(options: Options<ListHubRowsData, ThrowOnError>): RequestResult<ListHubRowsResponses, ListHubRowsErrors, ThrowOnError> => (options.client ?? client).get<ListHubRowsResponses, ListHubRowsErrors, ThrowOnError>({ url: '/api/v1/dataset-hubs/rows', ...options });

/**
 * Search the configured hubs.
 *
 * Always 200 when at least one hub is configured, even if every one of them
 * failed: the per-hub status carries the failure. A search where Kaggle is
 * rate-limited and Hugging Face answered is a partial result, and returning a
 * 502 for it would throw away the half that worked.
 */
export const searchHubs = <ThrowOnError extends boolean = false>(options?: Options<SearchHubsData, ThrowOnError>): RequestResult<SearchHubsResponses, SearchHubsErrors, ThrowOnError> => (options?.client ?? client).get<SearchHubsResponses, SearchHubsErrors, ThrowOnError>({ url: '/api/v1/dataset-hubs/search', ...options });

/**
 * One immutable dataset version, returned in small slices for an interactive viewer.
 */
export const getDatasetRows = <ThrowOnError extends boolean = false>(options: Options<GetDatasetRowsData, ThrowOnError>): RequestResult<GetDatasetRowsResponses, GetDatasetRowsErrors, ThrowOnError> => (options.client ?? client).get<GetDatasetRowsResponses, GetDatasetRowsErrors, ThrowOnError>({ url: '/api/v1/dataset-rows', ...options });

/**
 * Publish explicitly labelled limited output. Sample metadata is required.
 * A separate route prevents older servers from ignoring a new sample field.
 */
export const publishDatasetSample = <ThrowOnError extends boolean = false>(options: Options<PublishDatasetSampleData, ThrowOnError>): RequestResult<PublishDatasetSampleResponses, PublishDatasetSampleErrors, ThrowOnError> => (options.client ?? client).post<PublishDatasetSampleResponses, PublishDatasetSampleErrors, ThrowOnError>({
    url: '/api/v1/dataset-samples',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every saved dataset, newest execution first.
 */
export const listDatasets = <ThrowOnError extends boolean = false>(options?: Options<ListDatasetsData, ThrowOnError>): RequestResult<ListDatasetsResponses, ListDatasetsErrors, ThrowOnError> => (options?.client ?? client).get<ListDatasetsResponses, ListDatasetsErrors, ThrowOnError>({ url: '/api/v1/datasets', ...options });

/**
 * Persist exact execution output as an immutable version, optionally labelled as a sample.
 */
export const publishDataset = <ThrowOnError extends boolean = false>(options: Options<PublishDatasetData, ThrowOnError>): RequestResult<PublishDatasetResponses, PublishDatasetErrors, ThrowOnError> => (options.client ?? client).post<PublishDatasetResponses, PublishDatasetErrors, ThrowOnError>({
    url: '/api/v1/datasets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One dimension's rows: the explorer's top level, whatever it is rooted on.
 *
 * `session`, `agent`, `runtime`, `workflow`, `trace`, `model` and `tool` all
 * return the same row shape, so the panel's tree has one renderer rather than
 * one per pivot. See `aiwatcher_projector::dimensions`.
 */
export const listDimension = <ThrowOnError extends boolean = false>(options: Options<ListDimensionData, ThrowOnError>): RequestResult<ListDimensionResponses, ListDimensionErrors, ThrowOnError> => (options.client ?? client).get<ListDimensionResponses, ListDimensionErrors, ThrowOnError>({ url: '/api/v1/dimensions/{kind}', ...options });

/**
 * Every line, withdrawn ones included.
 */
export const listLines = <ThrowOnError extends boolean = false>(options?: Options<ListLinesData, ThrowOnError>): RequestResult<ListLinesResponses, ListLinesErrors, ThrowOnError> => (options?.client ?? client).get<ListLinesResponses, ListLinesErrors, ThrowOnError>({ url: '/api/v1/evaluation-approval-lines', ...options });

/**
 * Admit every variant of one experiment measured in one context.
 *
 * Sent any declaration of the line; its context and its variant's
 * `experiment_id` are what is admitted. `Admin`, for a pair's reason. A
 * scoring run of a variant the line covers is then admitted when it starts,
 * from the bytes its pipeline staged, and the approval names the line.
 */
export const admitLine = <ThrowOnError extends boolean = false>(options: Options<AdmitLineData, ThrowOnError>): RequestResult<AdmitLineResponses, AdmitLineErrors, ThrowOnError> => (options.client ?? client).post<AdmitLineResponses, AdmitLineErrors, ThrowOnError>({
    url: '/api/v1/evaluation-approval-lines',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stop a line admitting further variants. What it admitted stays admitted,
 * each withdrawable through its own approval.
 */
export const withdrawLine = <ThrowOnError extends boolean = false>(options: Options<WithdrawLineData, ThrowOnError>): RequestResult<WithdrawLineResponses, WithdrawLineErrors, ThrowOnError> => (options.client ?? client).delete<WithdrawLineResponses, WithdrawLineErrors, ThrowOnError>({ url: '/api/v1/evaluation-approval-lines/{line_id}', ...options });

/**
 * Every pair this instance has admitted, withdrawn ones included: an approval
 * that vanished from the list would read as one nobody ever made.
 */
export const listApprovals = <ThrowOnError extends boolean = false>(options?: Options<ListApprovalsData, ThrowOnError>): RequestResult<ListApprovalsResponses, ListApprovalsErrors, ThrowOnError> => (options?.client ?? client).get<ListApprovalsResponses, ListApprovalsErrors, ThrowOnError>({ url: '/api/v1/evaluation-approvals', ...options });

/**
 * Admit one pinned pair.
 *
 * The operator act that authorises publication, separated from publishing so
 * that every later repetition — from a worker, a schedule or CI — needs
 * nothing on the server's host. `Admin`, not `Editor`: an ingest token is an
 * editor by construction, and a producer that can admit its own evidence is
 * not an approval. Idempotent for the pair; a bundle that changed underneath
 * an admitted pair conflicts rather than moving what earlier results mean.
 */
export const approveSource = <ThrowOnError extends boolean = false>(options: Options<ApproveSourceData, ThrowOnError>): RequestResult<ApproveSourceResponses, ApproveSourceErrors, ThrowOnError> => (options.client ?? client).post<ApproveSourceResponses, ApproveSourceErrors, ThrowOnError>({
    url: '/api/v1/evaluation-approvals',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The address one declaration's bundle is staged under, and the pair it names.
 *
 * A caller cannot work this out: it is a digest over the canonicalised
 * declaration, and a second implementation of that in another language is a
 * second answer to what a pair *is*. Pure — it reads no store and admits
 * nothing — so it is the one route here below `admin`, and a declaration it
 * refuses is a 400 saying what is wrong with it rather than a digest of
 * something invalid.
 */
export const addressApproval = <ThrowOnError extends boolean = false>(options: Options<AddressApprovalData, ThrowOnError>): RequestResult<AddressApprovalResponses, AddressApprovalErrors, ThrowOnError> => (options.client ?? client).post<AddressApprovalResponses, AddressApprovalErrors, ThrowOnError>({
    url: '/api/v1/evaluation-approvals/address',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Withdraw one. Every result measured under that pair stops being readable and
 * no new one can be published; nothing already published has its retention
 * moved, in either direction. Final for this approval ID.
 */
export const withdrawApproval = <ThrowOnError extends boolean = false>(options: Options<WithdrawApprovalData, ThrowOnError>): RequestResult<WithdrawApprovalResponses, WithdrawApprovalErrors, ThrowOnError> => (options.client ?? client).delete<WithdrawApprovalResponses, WithdrawApprovalErrors, ThrowOnError>({ url: '/api/v1/evaluation-approvals/{approval_id}', ...options });

/**
 * Remove every staged member of one pair.
 *
 * What it does not do is un-admit anything: an approval is withdrawn through
 * its own route, and evidence already published under this pair keeps reading
 * until the adapter is asked for bytes that are no longer there. Use it to
 * correct a bundle before approving, or to clear one that was withdrawn.
 */
export const discardBundle = <ThrowOnError extends boolean = false>(options: Options<DiscardBundleData, ThrowOnError>): RequestResult<DiscardBundleResponses, DiscardBundleErrors, ThrowOnError> => (options.client ?? client).delete<DiscardBundleResponses, DiscardBundleErrors, ThrowOnError>({ url: '/api/v1/evaluation-approvals/{approval_id}/bundle', ...options });

/**
 * What is staged for one pair. Names and sizes: the digests are the
 * declaration's, and a second copy of them here could only disagree.
 */
export const listBundle = <ThrowOnError extends boolean = false>(options: Options<ListBundleData, ThrowOnError>): RequestResult<ListBundleResponses, ListBundleErrors, ThrowOnError> => (options.client ?? client).get<ListBundleResponses, ListBundleErrors, ThrowOnError>({ url: '/api/v1/evaluation-approvals/{approval_id}/bundle', ...options });

/**
 * Stage one member of a bundle, by the name the declaration gives it.
 *
 * This is what lets a *new* pair be admitted with nobody on the server's
 * host. Admin, because approving is: an ingest token is an editor by
 * construction, and bytes that decide what may be published are the operator's
 * act rather than the producer's.
 *
 * It admits nothing on its own. The approval that follows resolves the whole
 * bundle and records its digest, so bytes staged after one do not widen it —
 * they stop that pair reading until they are what was admitted again.
 */
export const stageBundle = <ThrowOnError extends boolean = false>(options: Options<StageBundleData, ThrowOnError>): RequestResult<StageBundleResponses, StageBundleErrors, ThrowOnError> => (options.client ?? client).put<StageBundleResponses, StageBundleErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/evaluation-approvals/{approval_id}/bundle/{name}',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * Every standing judgement about one thing, at its current revision.
 */
export const listAssessments = <ThrowOnError extends boolean = false>(options: Options<ListAssessmentsData, ThrowOnError>): RequestResult<ListAssessmentsResponses, ListAssessmentsErrors, ThrowOnError> => (options.client ?? client).get<ListAssessmentsResponses, ListAssessmentsErrors, ThrowOnError>({ url: '/api/v1/evaluation-assessments', ...options });

/**
 * Record one judgement.
 *
 * The caller is who filed it, always: a person's judgement is attributed to
 * the session, and a judge's names the judge while the session still says who
 * ran it. Nothing here checks the target exists — a judgement outlives the
 * trace it is about, which is the whole reason it is written down.
 *
 * Safe to send twice: repeating what the current revision already says lands
 * on that revision rather than recording a second one.
 */
export const recordAssessment = <ThrowOnError extends boolean = false>(options: Options<RecordAssessmentData, ThrowOnError>): RequestResult<RecordAssessmentResponses, RecordAssessmentErrors, ThrowOnError> => (options.client ?? client).post<RecordAssessmentResponses, RecordAssessmentErrors, ThrowOnError>({
    url: '/api/v1/evaluation-assessments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * What one standing judgement said over time, newest first.
 *
 * Both IDs came from the listing. A browser deriving either would be a second
 * answer to what one thing is, which is the rule an approval's address keeps.
 */
export const getAssessmentHistory = <ThrowOnError extends boolean = false>(options: Options<GetAssessmentHistoryData, ThrowOnError>): RequestResult<GetAssessmentHistoryResponses, GetAssessmentHistoryErrors, ThrowOnError> => (options.client ?? client).get<GetAssessmentHistoryResponses, GetAssessmentHistoryErrors, ThrowOnError>({ url: '/api/v1/evaluation-assessments/{target_id}/{standing_id}', ...options });

/**
 * Freeze what people judged of one result, as a judge's calibration set.
 *
 * Only a person's judgement, and only under the rubric versions named: a
 * judgement made under other words answered another question. Addressed by
 * its content, so taking the same judgements twice is the same set — and a
 * person changing their mind later is a later set, never this one re-read.
 */
export const takeCalibration = <ThrowOnError extends boolean = false>(options: Options<TakeCalibrationData, ThrowOnError>): RequestResult<TakeCalibrationResponses, TakeCalibrationErrors, ThrowOnError> => (options.client ?? client).post<TakeCalibrationResponses, TakeCalibrationErrors, ThrowOnError>({
    url: '/api/v1/evaluation-calibrations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One calibration set, at the address a declaration names it by.
 */
export const getCalibration = <ThrowOnError extends boolean = false>(options: Options<GetCalibrationData, ThrowOnError>): RequestResult<GetCalibrationResponses, GetCalibrationErrors, ThrowOnError> => (options.client ?? client).get<GetCalibrationResponses, GetCalibrationErrors, ThrowOnError>({ url: '/api/v1/evaluation-calibrations/{version}', ...options });

/**
 * Take a cohort from a dataset version this deployment owns.
 *
 * The first `limit` cases of a version's split, as the owner holds them, with
 * the three files a cohort pins derived here — so a declaration names them
 * and nobody writes or stages them. Admitting the pair derives them again from
 * the owner. A conversation corpus's cases are content, so an admin takes a
 * cohort from one, as an admin reads its cases. Project routes currently support
 * curation versions and annotation exports only; conversations remain unavailable.
 */
export const deriveCohort = <ThrowOnError extends boolean = false>(options: Options<DeriveCohortData, ThrowOnError>): RequestResult<DeriveCohortResponses, DeriveCohortErrors, ThrowOnError> => (options.client ?? client).post<DeriveCohortResponses, DeriveCohortErrors, ThrowOnError>({
    url: '/api/v1/evaluation-cohorts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Where the cases under one digest were derived from, if this deployment
 * derived them.
 */
export const getDerivedCohort = <ThrowOnError extends boolean = false>(options: Options<GetDerivedCohortData, ThrowOnError>): RequestResult<GetDerivedCohortResponses, GetDerivedCohortErrors, ThrowOnError> => (options.client ?? client).get<GetDerivedCohortResponses, GetDerivedCohortErrors, ThrowOnError>({ url: '/api/v1/evaluation-cohorts/{cases}', ...options });

/**
 * Download a staged answer document after verifying its SHA-256 digest.
 * Returns the original JSON bytes, preserving number spelling and whitespace.
 */
export const getRecording = <ThrowOnError extends boolean = false>(options: Options<GetRecordingData, ThrowOnError>): RequestResult<GetRecordingResponses, GetRecordingErrors, ThrowOnError> => (options.client ?? client).get<GetRecordingResponses, GetRecordingErrors, ThrowOnError>({ url: '/api/v1/evaluation-recordings/{digest}/content', ...options });

/**
 * Keep answers and return a reference to their exact bytes. The name is display
 * metadata, never a storage key. Retrying the same bytes preserves their digest.
 * On project routes the reference is interpreted only in that same project.
 */
export const stageRecording = <ThrowOnError extends boolean = false>(options: Options<StageRecordingData, ThrowOnError>): RequestResult<StageRecordingResponses, StageRecordingErrors, ThrowOnError> => (options.client ?? client).put<StageRecordingResponses, StageRecordingErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/evaluation-recordings/{name}',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * Durable discovery is independent of telemetry retention. Newest first, and
 * clients page with the opaque cursor rather than a timestamp assumption.
 * Conversation content requires Admin; other readers receive a forbidden state.
 */
export const listResults = <ThrowOnError extends boolean = false>(options?: Options<ListResultsData, ThrowOnError>): RequestResult<ListResultsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListResultsResponses, unknown, ThrowOnError>({ url: '/api/v1/evaluation-results', ...options });

/**
 * Publish terminal evidence. Retry the identical body and logical ID after a
 * timeout; a different body conflicts. No result content enters the event log.
 * Conversation evidence requires Admin, including publication.
 *
 * A pair nobody has admitted yet is a 409 `pair_not_admitted` naming the
 * approval, as starting a scoring run is; a withdrawn pair stays a 403.
 */
export const publishResult = <ThrowOnError extends boolean = false>(options: Options<PublishResultData, ThrowOnError>): RequestResult<PublishResultResponses, PublishResultErrors, ThrowOnError> => (options.client ?? client).post<PublishResultResponses, PublishResultErrors, ThrowOnError>({
    url: '/api/v1/evaluation-results',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Forget one published result.
 *
 * The third way evidence disappears, beside its approval being withdrawn and
 * its retention running out — and the only one that is about a single
 * measurement. It writes the same durable marker the retention sweep does, so
 * the ID stays permanently unreadable rather than falling back to telemetry.
 */
export const forgetResult = <ThrowOnError extends boolean = false>(options: Options<ForgetResultData, ThrowOnError>): RequestResult<ForgetResultResponses, ForgetResultErrors, ThrowOnError> => (options.client ?? client).delete<ForgetResultResponses, ForgetResultErrors, ThrowOnError>({ url: '/api/v1/evaluation-results/{evaluation_id}', ...options });

export const getResult = <ThrowOnError extends boolean = false>(options: Options<GetResultData, ThrowOnError>): RequestResult<GetResultResponses, GetResultErrors, ThrowOnError> => (options.client ?? client).get<GetResultResponses, GetResultErrors, ThrowOnError>({ url: '/api/v1/evaluation-results/{evaluation_id}', ...options });

export const getCases = <ThrowOnError extends boolean = false>(options: Options<GetCasesData, ThrowOnError>): RequestResult<GetCasesResponses, GetCasesErrors, ThrowOnError> => (options.client ?? client).get<GetCasesResponses, GetCasesErrors, ThrowOnError>({ url: '/api/v1/evaluation-results/{evaluation_id}/cases', ...options });

/**
 * One published result against another named one.
 *
 * Two headers, not two results: the metrics a comparison subtracts are in the
 * metadata each side was published with, so this costs what two summaries
 * cost however many cases are behind them. Which two may be subtracted is the
 * server's answer — `comparability` with its reasons, the same three words the
 * folded half uses — and a delta is withheld rather than shown wrong. Case
 * level regressions are not here: they are a full read of both sides.
 */
export const compareResults = <ThrowOnError extends boolean = false>(options: Options<CompareResultsData, ThrowOnError>): RequestResult<CompareResultsResponses, CompareResultsErrors, ThrowOnError> => (options.client ?? client).get<CompareResultsResponses, CompareResultsErrors, ThrowOnError>({ url: '/api/v1/evaluation-results/{evaluation_id}/comparison', ...options });

/**
 * Which cases moved between one published result and another named one.
 *
 * The comparison beside this one reads two headers; this reads both results,
 * so it pages, it is narrowed here rather than in the browser, and one
 * request walks a bounded number of cases. The verdict rides along, so a pair
 * this server will not subtract is refused in the same sentence the header
 * comparison uses rather than as an empty list — and an unverified pair keeps
 * its rows, because the cases that failed are what somebody opens this for.
 */
export const compareCases = <ThrowOnError extends boolean = false>(options: Options<CompareCasesData, ThrowOnError>): RequestResult<CompareCasesResponses, CompareCasesErrors, ThrowOnError> => (options.client ?? client).get<CompareCasesResponses, CompareCasesErrors, ThrowOnError>({ url: '/api/v1/evaluation-results/{evaluation_id}/comparison/cases', ...options });

/**
 * Whether a result may ship against a baseline, under a policy.
 *
 * A read: nothing is written, so a pipeline asks as often as it likes and the
 * panel asks the same thing. `pass`, `regression`, `incomplete` or `error`,
 * with every reason in words — a metric worse than its tolerance, a critical
 * case lost whatever the average did, a scorer that failed, a pair that does
 * not compare.
 */
export const gateResult = <ThrowOnError extends boolean = false>(options: Options<GateResultData, ThrowOnError>): RequestResult<GateResultResponses, GateResultErrors, ThrowOnError> => (options.client ?? client).post<GateResultResponses, GateResultErrors, ThrowOnError>({
    url: '/api/v1/evaluation-results/{evaluation_id}/gate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every proposal for one dataset, oldest first, at its current revision.
 */
export const listReviews = <ThrowOnError extends boolean = false>(options: Options<ListReviewsData, ThrowOnError>): RequestResult<ListReviewsResponses, ListReviewsErrors, ThrowOnError> => (options.client ?? client).get<ListReviewsResponses, ListReviewsErrors, ThrowOnError>({ url: '/api/v1/evaluation-reviews', ...options });

/**
 * Propose a case. Proposing what was noticed on the same target for the same
 * dataset again answers the review already under way, 200 rather than 201.
 *
 * Without a question, a case target is read at `at` — the position a
 * comparison row carries — for what the cohort asked and what the variant
 * answered, and the proposal is `measured`. A trace holds no words to read.
 * Project routes currently require supplied `written` or `observed` words;
 * result-source resolution remains unavailable in that scope.
 */
export const proposeCase = <ThrowOnError extends boolean = false>(options: Options<ProposeCaseData, ThrowOnError>): RequestResult<ProposeCaseResponses, ProposeCaseErrors, ThrowOnError> => (options.client ?? client).post<ProposeCaseResponses, ProposeCaseErrors, ThrowOnError>({
    url: '/api/v1/evaluation-reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every proposal seen on one target — a trace, a span, a session or a case
 * of a result — whichever dataset each would join, at its current revision.
 * What a case's judgements read to say the case is already under review.
 */
export const reviewsOfTarget = <ThrowOnError extends boolean = false>(options: Options<ReviewsOfTargetData, ThrowOnError>): RequestResult<ReviewsOfTargetResponses, ReviewsOfTargetErrors, ThrowOnError> => (options.client ?? client).get<ReviewsOfTargetResponses, ReviewsOfTargetErrors, ThrowOnError>({ url: '/api/v1/evaluation-reviews/of-target', ...options });

/**
 * Publish every approved case as a new version of its dataset: the current
 * version's rows, then one row per case, `review-…` by its ID.
 *
 * A dataset whose rows are not cases — `case_id`, `input`, `expected` — is
 * refused rather than given rows of a second shape. Publishing the same
 * approved set again lands on the same version.
 */
export const publishReviews = <ThrowOnError extends boolean = false>(options: Options<PublishReviewsData, ThrowOnError>): RequestResult<PublishReviewsResponses, PublishReviewsErrors, ThrowOnError> => (options.client ?? client).post<PublishReviewsResponses, PublishReviewsErrors, ThrowOnError>({ url: '/api/v1/evaluation-reviews/publish', ...options });

/**
 * Write an expected answer, approve or reject. Approving words somebody using
 * the application said — `observed` content — takes the admin role of the
 * project on project routes, or the instance on legacy routes.
 */
export const reviewCase = <ThrowOnError extends boolean = false>(options: Options<ReviewCaseData, ThrowOnError>): RequestResult<ReviewCaseResponses, ReviewCaseErrors, ThrowOnError> => (options.client ?? client).post<ReviewCaseResponses, ReviewCaseErrors, ThrowOnError>({
    url: '/api/v1/evaluation-reviews/{id}/actions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every form this instance holds, at its current version.
 */
export const listRubrics = <ThrowOnError extends boolean = false>(options?: Options<ListRubricsData, ThrowOnError>): RequestResult<ListRubricsResponses, ListRubricsErrors, ThrowOnError> => (options?.client ?? client).get<ListRubricsResponses, ListRubricsErrors, ThrowOnError>({ url: '/api/v1/evaluation-rubrics', ...options });

/**
 * Publish a form. Idempotent by content: the same words answer with the
 * version that is already there, and the head moves to it either way.
 */
export const publishRubric = <ThrowOnError extends boolean = false>(options: Options<PublishRubricData, ThrowOnError>): RequestResult<PublishRubricResponses, PublishRubricErrors, ThrowOnError> => (options.client ?? client).post<PublishRubricResponses, PublishRubricErrors, ThrowOnError>({
    url: '/api/v1/evaluation-rubrics',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One form, at the version asked for or at the head.
 */
export const getRubric = <ThrowOnError extends boolean = false>(options: Options<GetRubricData, ThrowOnError>): RequestResult<GetRubricResponses, GetRubricErrors, ThrowOnError> => (options.client ?? client).get<GetRubricResponses, GetRubricErrors, ThrowOnError>({ url: '/api/v1/evaluation-rubrics/{name}', ...options });

/**
 * Declare a measurement.
 *
 * Writes nothing that runs. What comes back is the declaration with the
 * manifest a result of it publishes and the approval that admits that pair,
 * because an operator has to admit it before it may publish and the metrics
 * in that manifest are derived from the card — a second copy written out by
 * hand is a second answer to what this run measures. Idempotent by content.
 *
 * The card is resolved here rather than at score time, so a version nobody
 * published is a refusal now instead of a run that fails in a minute.
 * Project declarations currently support saved recordings over native cohorts
 * with built-in scorers only. They neither admit nor start a measurement.
 */
export const declareScoringRun = <ThrowOnError extends boolean = false>(options: Options<DeclareScoringRunData, ThrowOnError>): RequestResult<DeclareScoringRunResponses, DeclareScoringRunErrors, ThrowOnError> => (options.client ?? client).post<DeclareScoringRunResponses, DeclareScoringRunErrors, ThrowOnError>({
    url: '/api/v1/evaluation-runs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * What a run measures, what it publishes, and whether it may yet.
 */
export const getScoringRun = <ThrowOnError extends boolean = false>(options: Options<GetScoringRunData, ThrowOnError>): RequestResult<GetScoringRunResponses, GetScoringRunErrors, ThrowOnError> => (options.client ?? client).get<GetScoringRunResponses, GetScoringRunErrors, ThrowOnError>({ url: '/api/v1/evaluation-runs/{id}', ...options });

/**
 * Start the declared measurement.
 *
 * Refused while nothing admits the pair, naming the approval that would. A
 * run started without one could only fail when it tried to publish, and that
 * failed run would then be what every later start of the same declaration
 * lands on. Once admitted, repeating this reaches the run already going.
 */
export const startScoringRun = <ThrowOnError extends boolean = false>(options: Options<StartScoringRunData, ThrowOnError>): RequestResult<StartScoringRunResponses, StartScoringRunErrors, ThrowOnError> => (options.client ?? client).post<StartScoringRunResponses, StartScoringRunErrors, ThrowOnError>({ url: '/api/v1/evaluation-runs/{id}/start', ...options });

/**
 * Every scorecard this instance holds, at its current version.
 *
 * A head carries the metrics its version declares, so a form choosing what to
 * run says what each card would measure without opening it.
 */
export const listScorecards = <ThrowOnError extends boolean = false>(options?: Options<ListScorecardsData, ThrowOnError>): RequestResult<ListScorecardsResponses, ListScorecardsErrors, ThrowOnError> => (options?.client ?? client).get<ListScorecardsResponses, ListScorecardsErrors, ThrowOnError>({ url: '/api/v1/evaluation-scorecards', ...options });

/**
 * Publish a scorecard. Idempotent by content: the same measurements answer
 * with the version that is already there, and the head moves to it either
 * way.
 *
 * Nothing here is code. A scorer is a name this deployment implements and the
 * parameters that name takes, so publishing a card is not a way to run
 * something on the server's host.
 */
export const publishScorecard = <ThrowOnError extends boolean = false>(options: Options<PublishScorecardData, ThrowOnError>): RequestResult<PublishScorecardResponses, PublishScorecardErrors, ThrowOnError> => (options.client ?? client).post<PublishScorecardResponses, PublishScorecardErrors, ThrowOnError>({
    url: '/api/v1/evaluation-scorecards',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One scorecard, at the version asked for or at the head.
 */
export const getScorecard = <ThrowOnError extends boolean = false>(options: Options<GetScorecardData, ThrowOnError>): RequestResult<GetScorecardResponses, GetScorecardErrors, ThrowOnError> => (options.client ?? client).get<GetScorecardResponses, GetScorecardErrors, ThrowOnError>({ url: '/api/v1/evaluation-scorecards/{name}', ...options });

/**
 * What changed from one version of a card to another: the metrics added,
 * removed and changed, each changed field of a scorer by JSON pointer, and
 * what each metric was derived to be on both sides — the part a version does
 * not hold.
 */
export const diffScorecard = <ThrowOnError extends boolean = false>(options: Options<DiffScorecardData, ThrowOnError>): RequestResult<DiffScorecardResponses, DiffScorecardErrors, ThrowOnError> => (options.client ?? client).get<DiffScorecardResponses, DiffScorecardErrors, ThrowOnError>({ url: '/api/v1/evaluation-scorecards/{name}/diff', ...options });

/**
 * Every version of one card, newest first — each whole, so a form can start
 * from any of them and a reader can pick two to compare.
 */
export const listScorecardVersions = <ThrowOnError extends boolean = false>(options: Options<ListScorecardVersionsData, ThrowOnError>): RequestResult<ListScorecardVersionsResponses, ListScorecardVersionsErrors, ThrowOnError> => (options.client ?? client).get<ListScorecardVersionsResponses, ListScorecardVersionsErrors, ThrowOnError>({ url: '/api/v1/evaluation-scorecards/{name}/versions', ...options });

/**
 * What the scorer service says it measures, as the work role last recorded it.
 *
 * The vocabulary a card's external metrics are named from: each adapter at the
 * release it runs, the model its graded metrics ask, and every metric with its
 * unit, direction, what it reads and the parameters it takes. A card is pinned
 * against this when it is published.
 */
export const getScorerCatalog = <ThrowOnError extends boolean = false>(options?: Options<GetScorerCatalogData, ThrowOnError>): RequestResult<GetScorerCatalogResponses, GetScorerCatalogErrors, ThrowOnError> => (options?.client ?? client).get<GetScorerCatalogResponses, GetScorerCatalogErrors, ThrowOnError>({ url: '/api/v1/evaluation-scorers', ...options });

/**
 * Suites: the level above a report, and what MLflow calls an experiment.
 *
 * A separate resource rather than `/evaluations/suites`, so an evaluation
 * that happens to be called `suites` is still reachable.
 */
export const listEvaluationSuites = <ThrowOnError extends boolean = false>(options?: Options<ListEvaluationSuitesData, ThrowOnError>): RequestResult<ListEvaluationSuitesResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListEvaluationSuitesResponses, unknown, ThrowOnError>({ url: '/api/v1/evaluation-suites', ...options });

/**
 * Keep one file a variant pins, by its content.
 *
 * `Editor`: the bytes are addressed by the digest computed here, so sending
 * them admits nothing — a line reads them when a run of that variant starts,
 * and a pin names them or nothing.
 */
export const stageVariantArtifact = <ThrowOnError extends boolean = false>(options: Options<StageVariantArtifactData, ThrowOnError>): RequestResult<StageVariantArtifactResponses, StageVariantArtifactErrors, ThrowOnError> => (options.client ?? client).put<StageVariantArtifactResponses, StageVariantArtifactErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/evaluation-variant-artifacts/{name}',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * Evaluation reports, newest first.
 *
 * The other half of the loop the traces come from: a trace says what one run
 * did, an evaluation says whether the thing producing those runs is getting
 * better. They arrive on the same log and are folded apart — an `eval.*`
 * event produces no span and no row in the runs list. See
 * `aiwatcher_projector::evaluations`.
 */
export const listEvaluations = <ThrowOnError extends boolean = false>(options?: Options<ListEvaluationsData, ThrowOnError>): RequestResult<ListEvaluationsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListEvaluationsResponses, unknown, ThrowOnError>({ url: '/api/v1/evaluations', ...options });

/**
 * One evaluation: its parameters, its metrics, its cases, the document the
 * producer attached, and the previous evaluation of the same suite on the
 * same dataset.
 */
export const getEvaluation = <ThrowOnError extends boolean = false>(options: Options<GetEvaluationData, ThrowOnError>): RequestResult<GetEvaluationResponses, GetEvaluationErrors, ThrowOnError> => (options.client ?? client).get<GetEvaluationResponses, GetEvaluationErrors, ThrowOnError>({ url: '/api/v1/evaluations/{evaluation_id}', ...options });

/**
 * Publish events over HTTP.
 *
 * The fallback path for clients that cannot reach Laser. Returns 403 when the
 * instance is configured without a sink.
 *
 * Writer, not viewer: this is the one read-model route that puts something in
 * the durable log, and an agent that publishes here is a machine identity —
 * an authentik service account holding a token for this audience, or a
 * bearer the operator issued. Reading runs and writing them are different
 * permissions in every deployment that has more than one team.
 *
 * Every event is recorded as published by that identity — the token's name or
 * the person's subject — whatever the body says, which is what lets a reader
 * tell one publisher's word from another's.
 *
 * The same rule decides **which project** an event belongs to, and here it is
 * load-bearing rather than descriptive. The credential's scope is written onto
 * every envelope in the batch, including when the credential has none, so a
 * producer that named a project is ignored and one that named somebody else's
 * has written nothing into it. Absence means the global log, which is what
 * every token without a project does and what every event before this field
 * existed is (ADR_0033 pt. 3).
 */
export const ingest = <ThrowOnError extends boolean = false>(options: Options<IngestData, ThrowOnError>): RequestResult<IngestResponses, IngestErrors, ThrowOnError> => (options.client ?? client).post<IngestResponses, IngestErrors, ThrowOnError>({
    url: '/api/v1/events',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Server-sent events for the whole system: a catch-up marker, then live.
 *
 * This is the panel's Observability transport. It is deliberately separate
 * from the WebSocket below: the panel only receives here, so EventSource can
 * own reconnects and `Last-Event-ID` resume without client-side machinery.
 */
export const streamEvents = <ThrowOnError extends boolean = false>(options: Options<StreamEventsData, ThrowOnError>): RequestResult<StreamEventsResponses, unknown, ThrowOnError> => (options.client ?? client).get<StreamEventsResponses, unknown, ThrowOnError>({ url: '/api/v1/events/stream', ...options });

/**
 * Compile a definition and start running it.
 *
 * `202`, not `200`: what is durable when this answers is the command and the
 * first decision, and nothing has executed. The browser may close immediately
 * afterwards — that is the whole point of the route, and of ADR_0025.
 */
export const startExecution = <ThrowOnError extends boolean = false>(options: Options<StartExecutionData, ThrowOnError>): RequestResult<StartExecutionResponses, StartExecutionErrors, ThrowOnError> => (options.client ?? client).post<StartExecutionResponses, StartExecutionErrors, ThrowOnError>({
    url: '/api/v1/executions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One run's own page: where it is, where each of its steps is, and what may be
 * done to it.
 *
 * The store's inline projection rather than a fold of the log, and the two are
 * not interchangeable. This one is transactional with the decision that
 * produced it, which is what makes it safe to accept the next command from;
 * the fold is what draws the run beside every other execution, with `Pending`
 * nodes and a live stream, and it is the one a list comes from.
 *
 * Answers [`RunView`] — the projection under `execution`, beside the actions
 * the run would accept. It answered the bare projection once; see that type
 * for what a hand-written client has to change.
 */
export const getExecution = <ThrowOnError extends boolean = false>(options: Options<GetExecutionData, ThrowOnError>): RequestResult<GetExecutionResponses, GetExecutionErrors, ThrowOnError> => (options.client ?? client).get<GetExecutionResponses, GetExecutionErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}', ...options });

/**
 * Everything one run produced, in the order it was recorded.
 *
 * Each row carries its `produced_by` — the execution, the step and the
 * attempt — which is what lets a step's view show its own outputs and its own
 * log without a second route per noun.
 *
 * A run that produced nothing and a run that never existed are the same empty
 * list. The catalog cannot tell them apart and neither could a caller that
 * asked it to: whether a run exists is `GET /executions/{id}`, and answering
 * it twice would be a second read of a second store to describe the same
 * absence.
 */
export const runArtifacts = <ThrowOnError extends boolean = false>(options: Options<RunArtifactsData, ThrowOnError>): RequestResult<RunArtifactsResponses, RunArtifactsErrors, ThrowOnError> => (options.client ?? client).get<RunArtifactsResponses, RunArtifactsErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/artifacts', ...options });

/**
 * One of this run's artifacts, read as text.
 *
 * The digest is checked against what this execution produced *before*
 * anything is read, so the route answers for a run's own outputs and for
 * nothing else in the store.
 */
export const artifactContent = <ThrowOnError extends boolean = false>(options: Options<ArtifactContentData, ThrowOnError>): RequestResult<ArtifactContentResponses, ArtifactContentErrors, ThrowOnError> => (options.client ?? client).get<ArtifactContentResponses, ArtifactContentErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/artifacts/{digest}', ...options });

/**
 * Which authored blocks became which steps, for the run the canvas is following.
 *
 * One request per execution rather than one per frame: a plan is immutable, so
 * this answer cannot change while the run does. The states themselves come
 * from `GET /executions/{id}`, which is re-read on every frame.
 */
export const runBlocks = <ThrowOnError extends boolean = false>(options: Options<RunBlocksData, ThrowOnError>): RequestResult<RunBlocksResponses, RunBlocksErrors, ThrowOnError> => (options.client ?? client).get<RunBlocksResponses, RunBlocksErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/blocks', ...options });

/**
 * Stop a run, and everything it has not already dispatched.
 */
export const cancelExecution = <ThrowOnError extends boolean = false>(options: Options<CancelExecutionData, ThrowOnError>): RequestResult<CancelExecutionResponses, CancelExecutionErrors, ThrowOnError> => (options.client ?? client).post<CancelExecutionResponses, CancelExecutionErrors, ThrowOnError>({
    url: '/api/v1/executions/{execution_id}/commands/cancel',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Schedule nothing further until this run is resumed.
 *
 * What is already dispatched keeps running: an attempt is somebody else's
 * process, and a pause that could reach into it would be a cancel wearing the
 * wrong name.
 */
export const pauseExecution = <ThrowOnError extends boolean = false>(options: Options<PauseExecutionData, ThrowOnError>): RequestResult<PauseExecutionResponses, PauseExecutionErrors, ThrowOnError> => (options.client ?? client).post<PauseExecutionResponses, PauseExecutionErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/commands/pause', ...options });

/**
 * Let a paused run schedule again.
 */
export const resumeExecution = <ThrowOnError extends boolean = false>(options: Options<ResumeExecutionData, ThrowOnError>): RequestResult<ResumeExecutionResponses, ResumeExecutionErrors, ThrowOnError> => (options.client ?? client).post<ResumeExecutionResponses, ResumeExecutionErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/commands/resume', ...options });

/**
 * Who is deciding this run, if anybody still is.
 *
 * The read behind "a lease is not something the claimant can check about
 * itself": the worker asks, and this answers from the row. 404 once it has run
 * out, because expired and never taken are the same answer to a caller — it is
 * free.
 */
export const readDeciderLease = <ThrowOnError extends boolean = false>(options: Options<ReadDeciderLeaseData, ThrowOnError>): RequestResult<ReadDeciderLeaseResponses, ReadDeciderLeaseErrors, ThrowOnError> => (options.client ?? client).get<ReadDeciderLeaseResponses, ReadDeciderLeaseErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/decider-lease', ...options });

/**
 * Take, or renew, the right to decide one hosted run.
 *
 * `agentic.workflow`'s `ProcessorLock`, in the store that holds the history.
 * Renewing is this same call under the same name, so a heartbeat and a first
 * claim cannot come to disagree.
 *
 * **200 either way.** Being told who holds it is an answer to the question
 * rather than a failure of it, and the alternative — a 409 whose structured
 * `expires_at` has to be smuggled through an error body — is worse for the one
 * caller that has to branch on it. What *is* a 409 is appending while somebody
 * else decides.
 */
export const takeDeciderLease = <ThrowOnError extends boolean = false>(options: Options<TakeDeciderLeaseData, ThrowOnError>): RequestResult<TakeDeciderLeaseResponses, TakeDeciderLeaseErrors, ThrowOnError> => (options.client ?? client).post<TakeDeciderLeaseResponses, TakeDeciderLeaseErrors, ThrowOnError>({
    url: '/api/v1/executions/{execution_id}/decider-lease',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Give up the right to decide.
 *
 * Worth calling rather than waiting the lease out: it is the difference
 * between a replacement starting now and starting in five minutes. `released`
 * is false when it was never this caller's to give — a worker that had already
 * been taken over must not release its replacement's lease.
 */
export const releaseDeciderLease = <ThrowOnError extends boolean = false>(options: Options<ReleaseDeciderLeaseData, ThrowOnError>): RequestResult<ReleaseDeciderLeaseResponses, ReleaseDeciderLeaseErrors, ThrowOnError> => (options.client ?? client).post<ReleaseDeciderLeaseResponses, ReleaseDeciderLeaseErrors, ThrowOnError>({
    url: '/api/v1/executions/{execution_id}/decider-lease/release',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const executionHistory = <ThrowOnError extends boolean = false>(options: Options<ExecutionHistoryData, ThrowOnError>): RequestResult<ExecutionHistoryResponses, ExecutionHistoryErrors, ThrowOnError> => (options.client ?? client).get<ExecutionHistoryResponses, ExecutionHistoryErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/history', ...options });

/**
 * Seal one hosted execution's payload under this instance's keys.
 *
 * The `sealed` half of a run's payload policy. `external` runs never call
 * this: their words stay wherever the worker keeps them, and this instance
 * holds a reference it cannot resolve — which is the free default and the one
 * a deployment gets by doing nothing.
 *
 * The body is the plaintext, as bytes. It is not JSON on purpose: what is
 * being stored is somebody's words, and re-encoding them on the way in would
 * mean the digest recorded in the stream is a digest of this route's idea of
 * them rather than of what was sent.
 */
export const sealPayload = <ThrowOnError extends boolean = false>(options: Options<SealPayloadData, ThrowOnError>): RequestResult<SealPayloadResponses, SealPayloadErrors, ThrowOnError> => (options.client ?? client).post<SealPayloadResponses, SealPayloadErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/executions/{execution_id}/payloads',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * Read one back.
 *
 * An `admin`, as reading a turn's content is, and for the same reason: these
 * are somebody's words, and the only thing that changed by sealing them here
 * rather than leaving them with the worker is that this deployment took
 * responsibility for them.
 */
export const readPayload = <ThrowOnError extends boolean = false>(options: Options<ReadPayloadData, ThrowOnError>): RequestResult<ReadPayloadResponses, ReadPayloadErrors, ThrowOnError> => (options.client ?? client).get<ReadPayloadResponses, ReadPayloadErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/payloads/{digest}', ...options });

/**
 * Take one step again, from the inputs and the code its plan pinned.
 *
 * A new attempt rather than a rewrite: the failed one stays in the stream with
 * its error, which is what a waterfall draws and what a person reads to find
 * out why this needed a second go.
 */
export const retryStep = <ThrowOnError extends boolean = false>(options: Options<RetryStepData, ThrowOnError>): RequestResult<RetryStepResponses, RetryStepErrors, ThrowOnError> => (options.client ?? client).post<RetryStepResponses, RetryStepErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/steps/{step_id}/commands/retry', ...options });

/**
 * One step of one run, exactly as it was.
 *
 * Read from the stream rather than from the definition: the run pinned a plan,
 * and the definition may have moved on several revisions since. That is the
 * difference between reopening a block and reopening *this* block.
 */
export const stepContext = <ThrowOnError extends boolean = false>(options: Options<StepContextData, ThrowOnError>): RequestResult<StepContextResponses, StepContextErrors, ThrowOnError> => (options.client ?? client).get<StepContextResponses, StepContextErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/steps/{step_id}/context', ...options });

/**
 * Open this step's editor on the rows this step actually read.
 *
 * Resolved server-side and without a token — see
 * `aiwatcher_core::ports::EditorHost` for why one would have been ceremony.
 * The gate is here: this route decides who may open which run's rows, reads
 * them from the object store itself, and hands the runtime a staging request.
 * `Editor`, not `Viewer`, because staging replaces what every other person
 * looking at that notebook's live app is shown.
 *
 * It stages and stops. And the live app serves the notebook's *head*, so what
 * opens is this run's rows under today's code — `code_revision` in the answer
 * is what ran, read beside it through the runtime's revision route.
 */
export const openEditor = <ThrowOnError extends boolean = false>(options: Options<OpenEditorData, ThrowOnError>): RequestResult<OpenEditorResponses, OpenEditorErrors, ThrowOnError> => (options.client ?? client).post<OpenEditorResponses, OpenEditorErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/steps/{step_id}/editor', ...options });

/**
 * Answer the question a `HumanInput` step is waiting on.
 */
export const provideInput = <ThrowOnError extends boolean = false>(options: Options<ProvideInputData, ThrowOnError>): RequestResult<ProvideInputResponses, ProvideInputErrors, ThrowOnError> => (options.client ?? client).post<ProvideInputResponses, ProvideInputErrors, ThrowOnError>({
    url: '/api/v1/executions/{execution_id}/steps/{step_id}/input',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Append a worker's messages to a hosted execution's history.
 *
 * The decider is the worker; this is the shared history `agentic.workflow`
 * cannot give itself when every agent worker holds its own SQLite. What this
 * route contributes is the three things a private store has no way to offer: a
 * compare-and-append against a version, an inbox keyed by the
 * `Idempotency-Key`, and a 409 that says where the stream actually got to.
 *
 * It reads none of the messages. A `409` is not a failure to retry blindly:
 * the worker reloads and decides on what it now sees, and its own cached
 * decision across OCC retries is what stops that reload calling the model
 * again to discover it lost.
 */
export const appendStream = <ThrowOnError extends boolean = false>(options: Options<AppendStreamData, ThrowOnError>): RequestResult<AppendStreamResponses, AppendStreamErrors, ThrowOnError> => (options.client ?? client).post<AppendStreamResponses, AppendStreamErrors, ThrowOnError>({
    url: '/api/v1/executions/{execution_id}/stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The deferred appends this run is holding.
 *
 * A read, so a decider that restarted can see what it scheduled before it went
 * away — which is the question the whole of step 4 exists to make answerable.
 */
export const executionTimers = <ThrowOnError extends boolean = false>(options: Options<ExecutionTimersData, ThrowOnError>): RequestResult<ExecutionTimersResponses, ExecutionTimersErrors, ThrowOnError> => (options.client ?? client).get<ExecutionTimersResponses, ExecutionTimersErrors, ThrowOnError>({ url: '/api/v1/executions/{execution_id}/timers', ...options });

/**
 * The contexts results were published in, newest first — each an experiment:
 * what it measures, how many results and how many variants.
 */
export const listExperiments = <ThrowOnError extends boolean = false>(options?: Options<ListExperimentsData, ThrowOnError>): RequestResult<ListExperimentsResponses, ListExperimentsErrors, ThrowOnError> => (options?.client ?? client).get<ListExperimentsResponses, ListExperimentsErrors, ThrowOnError>({ url: '/api/v1/experiments', ...options });

/**
 * Every result published in one context, newest first, compared with the
 * baseline when one is named — and the whole-run timing of each managed run
 * behind them.
 */
export const getExperiment = <ThrowOnError extends boolean = false>(options: Options<GetExperimentData, ThrowOnError>): RequestResult<GetExperimentResponses, GetExperimentErrors, ThrowOnError> => (options.client ?? client).get<GetExperimentResponses, GetExperimentErrors, ThrowOnError>({ url: '/api/v1/experiments/{context_id}', ...options });

/**
 * Turn a token into membership and a grant, once.
 *
 * Any signed-in principal may call this — the token is the authority, and the
 * caller's verified pair is what the grant is written to. It names no
 * organization because whoever holds one does not know which organization it
 * belongs to, and a route that made them say would leak that they had guessed
 * right.
 */
export const redeem = <ThrowOnError extends boolean = false>(options: Options<RedeemData, ThrowOnError>): RequestResult<RedeemResponses, RedeemErrors, ThrowOnError> => (options.client ?? client).post<RedeemResponses, RedeemErrors, ThrowOnError>({
    url: '/api/v1/iam/invitations/redeem',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const organizations = <ThrowOnError extends boolean = false>(options?: Options<OrganizationsData, ThrowOnError>): RequestResult<OrganizationsResponses, OrganizationsErrors, ThrowOnError> => (options?.client ?? client).get<OrganizationsResponses, OrganizationsErrors, ThrowOnError>({ url: '/api/v1/iam/organizations', ...options });

/**
 * Bootstrap policy: an OIDC instance admin creates an organization owned by
 * themselves. A client cannot substitute an owner in the request body.
 */
export const createOrganization = <ThrowOnError extends boolean = false>(options: Options<CreateOrganizationData, ThrowOnError>): RequestResult<CreateOrganizationResponses, CreateOrganizationErrors, ThrowOnError> => (options.client ?? client).post<CreateOrganizationResponses, CreateOrganizationErrors, ThrowOnError>({
    url: '/api/v1/iam/organizations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const audit = <ThrowOnError extends boolean = false>(options: Options<AuditData, ThrowOnError>): RequestResult<AuditResponses, AuditErrors, ThrowOnError> => (options.client ?? client).get<AuditResponses, AuditErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/audit', ...options });

export const auditBounds = <ThrowOnError extends boolean = false>(options: Options<AuditBoundsData, ThrowOnError>): RequestResult<AuditBoundsResponses, AuditBoundsErrors, ThrowOnError> => (options.client ?? client).get<AuditBoundsResponses, AuditBoundsErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/audit/bounds', ...options });

export const auditExports = <ThrowOnError extends boolean = false>(options: Options<AuditExportsData, ThrowOnError>): RequestResult<AuditExportsResponses, AuditExportsErrors, ThrowOnError> => (options.client ?? client).get<AuditExportsResponses, AuditExportsErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/audit/exports', ...options });

/**
 * Queue a frozen copy of this organization's trail.
 *
 * The range is pinned here rather than by the worker, so a grant issued while
 * the job runs belongs to the next export. The response is the queued job: the
 * bytes arrive shard by shard, and `GET .../exports/{job}` is what says how far
 * it has got.
 */
export const createAuditExport = <ThrowOnError extends boolean = false>(options: Options<CreateAuditExportData, ThrowOnError>): RequestResult<CreateAuditExportResponses, CreateAuditExportErrors, ThrowOnError> => (options.client ?? client).post<CreateAuditExportResponses, CreateAuditExportErrors, ThrowOnError>({
    url: '/api/v1/iam/organizations/{organization}/audit/exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stop an export that has not finished. A finished one is 409: what it
 * produced is a record, and there is nothing left for a cancellation to
 * prevent.
 */
export const cancelAuditExport = <ThrowOnError extends boolean = false>(options: Options<CancelAuditExportData, ThrowOnError>): RequestResult<CancelAuditExportResponses, CancelAuditExportErrors, ThrowOnError> => (options.client ?? client).delete<CancelAuditExportResponses, CancelAuditExportErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/audit/exports/{job}', ...options });

export const auditExport = <ThrowOnError extends boolean = false>(options: Options<AuditExportData, ThrowOnError>): RequestResult<AuditExportResponses, AuditExportErrors, ThrowOnError> => (options.client ?? client).get<AuditExportResponses, AuditExportErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/audit/exports/{job}', ...options });

/**
 * The frozen entries, a page at a time.
 */
export const auditExportRows = <ThrowOnError extends boolean = false>(options: Options<AuditExportRowsData, ThrowOnError>): RequestResult<AuditExportRowsResponses, AuditExportRowsErrors, ThrowOnError> => (options.client ?? client).get<AuditExportRowsResponses, AuditExportRowsErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/audit/exports/{job}/rows', ...options });

export const apply = <ThrowOnError extends boolean = false>(options: Options<ApplyData, ThrowOnError>): RequestResult<ApplyResponses, ApplyErrors, ThrowOnError> => (options.client ?? client).post<ApplyResponses, ApplyErrors, ThrowOnError>({
    url: '/api/v1/iam/organizations/{organization}/commands',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The offers for projects this caller may administer, newest first.
 */
export const invitations = <ThrowOnError extends boolean = false>(options: Options<InvitationsData, ThrowOnError>): RequestResult<InvitationsResponses, InvitationsErrors, ThrowOnError> => (options.client ?? client).get<InvitationsResponses, InvitationsErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/invitations', ...options });

/**
 * Withdraw an offer nobody has taken up.
 *
 * A redeemed one answers 409 rather than disappearing: what it produced is a
 * grant, and taking that back is the revoke-grant command's job.
 */
export const revokeInvitation = <ThrowOnError extends boolean = false>(options: Options<RevokeInvitationData, ThrowOnError>): RequestResult<RevokeInvitationResponses, RevokeInvitationErrors, ThrowOnError> => (options.client ?? client).delete<RevokeInvitationResponses, RevokeInvitationErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/invitations/{invitation}', ...options });

export const projects = <ThrowOnError extends boolean = false>(options: Options<ProjectsData, ThrowOnError>): RequestResult<ProjectsResponses, ProjectsErrors, ThrowOnError> => (options.client ?? client).get<ProjectsResponses, ProjectsErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/projects', ...options });

export const access = <ThrowOnError extends boolean = false>(options: Options<AccessData, ThrowOnError>): RequestResult<AccessResponses, AccessErrors, ThrowOnError> => (options.client ?? client).get<AccessResponses, AccessErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/projects/{project}/access', ...options });

/**
 * Every grant on one project, for whoever may issue one there.
 *
 * The windows are as issued and nothing is filtered by the clock — what a
 * grant does *now* is the access route's answer, per person, and a list that
 * dropped a lapsed row would hide the thing somebody opened it to see.
 */
export const projectGrants = <ThrowOnError extends boolean = false>(options: Options<ProjectGrantsData, ThrowOnError>): RequestResult<ProjectGrantsResponses, ProjectGrantsErrors, ThrowOnError> => (options.client ?? client).get<ProjectGrantsResponses, ProjectGrantsErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/projects/{project}/grants', ...options });

/**
 * Offer a grant to somebody who may never have signed in here.
 *
 * A grant names a `(provider, subject)` pair, and nobody knows a stranger's
 * subject until their provider has minted one — so the offer is made to a
 * secret instead, and the pair is learned when it is redeemed. The token comes
 * back once, in this response, and nothing can show it again.
 */
export const invite = <ThrowOnError extends boolean = false>(options: Options<InviteData, ThrowOnError>): RequestResult<InviteResponses, InviteErrors, ThrowOnError> => (options.client ?? client).post<InviteResponses, InviteErrors, ThrowOnError>({
    url: '/api/v1/iam/organizations/{organization}/projects/{project}/invitations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Who is in this organization, for an owner or admin of it.
 *
 * The one read here that is not about the caller. `projects` and `access`
 * answer "what may I reach", which is the right shape for a permission check
 * and no use to somebody administering one: an organization admin may grant on
 * a project they hold no grant on, and without this could not name it.
 */
export const roster = <ThrowOnError extends boolean = false>(options: Options<RosterData, ThrowOnError>): RequestResult<RosterResponses, RosterErrors, ThrowOnError> => (options.client ?? client).get<RosterResponses, RosterErrors, ThrowOnError>({ url: '/api/v1/iam/organizations/{organization}/roster', ...options });

/**
 * Every lab in this workshop, by position and then by name.
 */
export const listLabs = <ThrowOnError extends boolean = false>(options?: Options<ListLabsData, ThrowOnError>): RequestResult<ListLabsResponses, ListLabsErrors, ThrowOnError> => (options?.client ?? client).get<ListLabsResponses, ListLabsErrors, ThrowOnError>({ url: '/api/v1/labs', ...options });

/**
 * Publish a version of a lab.
 *
 * Idempotent on the document: `created` is `false` when this exact lab was
 * already stored, and the version that comes back is the one that was there.
 *
 * A lab that pins tests is refused unless both halves resolve in this
 * project's evaluation registry — a stored lab that measures nothing is a
 * slot that reads as working and answers nothing when somebody submits.
 */
export const publishLab = <ThrowOnError extends boolean = false>(options: Options<PublishLabData, ThrowOnError>): RequestResult<PublishLabResponses, PublishLabErrors, ThrowOnError> => (options.client ?? client).post<PublishLabResponses, PublishLabErrors, ThrowOnError>({
    url: '/api/v1/labs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One lab: its versions, its labels, and the brief it is at now.
 */
export const getLab = <ThrowOnError extends boolean = false>(options: Options<GetLabData, ThrowOnError>): RequestResult<GetLabResponses, GetLabErrors, ThrowOnError> => (options.client ?? client).get<GetLabResponses, GetLabErrors, ThrowOnError>({ url: '/api/v1/labs/{name}', ...options });

/**
 * Move a label onto a version that is already stored.
 *
 * Its own route because writing a lesson and setting it are different
 * decisions — `POST /labs` with a `label` is the shorthand for doing both at
 * once, which is what a first publish wants.
 */
export const setLabLabel = <ThrowOnError extends boolean = false>(options: Options<SetLabLabelData, ThrowOnError>): RequestResult<SetLabLabelResponses, SetLabLabelErrors, ThrowOnError> => (options.client ?? client).put<SetLabLabelResponses, SetLabLabelErrors, ThrowOnError>({
    url: '/api/v1/labs/{name}/labels/{label}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The context every submission to this lab publishes under.
 *
 * `context_id` is what `GET /api/v1/evaluation-results?context_id=…` is
 * narrowed by, so this is the whole join from a lab to its marks. It is
 * answerable before anybody has submitted, which is the point.
 */
export const getLabMeasurement = <ThrowOnError extends boolean = false>(options: Options<GetLabMeasurementData, ThrowOnError>): RequestResult<GetLabMeasurementResponses, GetLabMeasurementErrors, ThrowOnError> => (options.client ?? client).get<GetLabMeasurementResponses, GetLabMeasurementErrors, ThrowOnError>({ url: '/api/v1/labs/{name}/measurement', ...options });

/**
 * One version, with its brief.
 */
export const getLabVersion = <ThrowOnError extends boolean = false>(options: Options<GetLabVersionData, ThrowOnError>): RequestResult<GetLabVersionResponses, GetLabVersionErrors, ThrowOnError> => (options.client ?? client).get<GetLabVersionResponses, GetLabVersionErrors, ThrowOnError>({ url: '/api/v1/labs/{name}/versions/{version_id}', ...options });

/**
 * A WebSocket for the whole system, or one run.
 *
 * Prefer SSE where the traffic is one-way — it reconnects on its own and
 * survives proxies better. Use this when the panel needs to send *up* as well:
 * cancelling a run, approving a tool call, submitting human feedback.
 */
export const liveWebsocket = <ThrowOnError extends boolean = false>(options?: Options<LiveWebsocketData, ThrowOnError>): RequestResult<unknown, unknown, ThrowOnError> => (options?.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/v1/live', ...options });

/**
 * Aggregates over the runs the projector still holds.
 *
 * Served from the read model rather than from a metrics backend: the numbers
 * are a fold over data already in memory, so the page renders with no PromQL
 * and no dependency on VictoriaMetrics being reachable. The window is bounded
 * by retention — `window.runs_retained` against `window.retention_limit` tells
 * a caller whether it is looking at everything or at a truncated tail.
 */
export const getMetrics = <ThrowOnError extends boolean = false>(options?: Options<GetMetricsData, ThrowOnError>): RequestResult<GetMetricsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetMetricsResponses, unknown, ThrowOnError>({ url: '/api/v1/metrics', ...options });

/**
 * Every registered model.
 */
export const listModels = <ThrowOnError extends boolean = false>(options?: Options<ListModelsData, ThrowOnError>): RequestResult<ListModelsResponses, ListModelsErrors, ThrowOnError> => (options?.client ?? client).get<ListModelsResponses, ListModelsErrors, ThrowOnError>({ url: '/api/v1/models', ...options });

/**
 * Register what a run produced.
 *
 * The provenance — dataset, framework, code — is read from the run rather
 * than from this request, so a version cannot claim a lineage the run it
 * names does not have. A version that cannot be promoted is still recorded,
 * and the reason comes back with it.
 */
export const registerModel = <ThrowOnError extends boolean = false>(options: Options<RegisterModelData, ThrowOnError>): RequestResult<RegisterModelResponses, RegisterModelErrors, ThrowOnError> => (options.client ?? client).post<RegisterModelResponses, RegisterModelErrors, ThrowOnError>({
    url: '/api/v1/models',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One model: its versions, its labels, and one version's record.
 */
export const getModel = <ThrowOnError extends boolean = false>(options: Options<GetModelData, ThrowOnError>): RequestResult<GetModelResponses, GetModelErrors, ThrowOnError> => (options.client ?? client).get<GetModelResponses, GetModelErrors, ThrowOnError>({ url: '/api/v1/models/{name}', ...options });

/**
 * Point a label at a version.
 *
 * The one write here that changes what a service loads next, so it needs
 * instance `admin` on legacy routes or project `admin` on scoped routes
 * — and the one the registry itself can refuse: a version with no
 * held-out measurement, or one trained on a dataset name nobody can
 * reconstruct, is not promotable however much anybody wants it to be.
 */
export const setModelLabel = <ThrowOnError extends boolean = false>(options: Options<SetModelLabelData, ThrowOnError>): RequestResult<SetModelLabelResponses, SetModelLabelErrors, ThrowOnError> => (options.client ?? client).post<SetModelLabelResponses, SetModelLabelErrors, ThrowOnError>({
    url: '/api/v1/models/{name}/labels',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Store an image under the digest of its own bytes.
 *
 * The digest is computed here. A content address supplied by the caller is
 * not a content address, and trusting one would let two different images
 * occupy the same key.
 */
export const projectUploadBlob = <ThrowOnError extends boolean = false>(options: Options<ProjectUploadBlobData, ThrowOnError>): RequestResult<ProjectUploadBlobResponses, ProjectUploadBlobErrors, ThrowOnError> => (options.client ?? client).post<ProjectUploadBlobResponses, ProjectUploadBlobErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/orgs/{organization}/projects/{project}/annotation-blobs',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * The bytes of an uploaded image.
 *
 * Immutable by construction — the key *is* the digest — so it is cached for a
 * year on legacy routes. Project routes override this with no-store so
 * revoked access is checked on the next read. An annotation canvas re-fetching a 2 MB plan on every pan would be the
 * slowest part of the tool.
 */
export const projectGetBlob = <ThrowOnError extends boolean = false>(options: Options<ProjectGetBlobData, ThrowOnError>): RequestResult<ProjectGetBlobResponses, ProjectGetBlobErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetBlobResponses, ProjectGetBlobErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-blobs/{image_id}', ...options });

/**
 * One immutable manifest: its samples, its splits, and every exclusion with
 * its reason.
 */
export const projectGetExport = <ThrowOnError extends boolean = false>(options: Options<ProjectGetExportData, ThrowOnError>): RequestResult<ProjectGetExportResponses, ProjectGetExportErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetExportResponses, ProjectGetExportErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-export', ...options });

/**
 * The same export as a COCO document.
 *
 * Generated rather than stored: COCO is a derived view of the vector
 * annotations, and storing it would be a second copy of the truth that can
 * disagree with the first. It reads one revision object per sample, which is
 * paid once per training run rather than once per epoch.
 */
export const projectGetExportCoco = <ThrowOnError extends boolean = false>(options: Options<ProjectGetExportCocoData, ThrowOnError>): RequestResult<ProjectGetExportCocoResponses, ProjectGetExportCocoErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetExportCocoResponses, ProjectGetExportCocoErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-export/coco', ...options });

/**
 * Every export built from a project, newest first.
 */
export const projectListExports = <ThrowOnError extends boolean = false>(options: Options<ProjectListExportsData, ThrowOnError>): RequestResult<ProjectListExportsResponses, ProjectListExportsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListExportsResponses, ProjectListExportsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-exports', ...options });

/**
 * Freeze the project as it stands into an immutable, content-addressed
 * manifest.
 *
 * The reference a training run records is `project@export`. Two exports of an
 * unchanged project are one export, which is why re-running this nightly is
 * cheap and why a manifest is a comparison boundary rather than a timestamp.
 */
export const projectBuildExport = <ThrowOnError extends boolean = false>(options: Options<ProjectBuildExportData, ThrowOnError>): RequestResult<ProjectBuildExportResponses, ProjectBuildExportErrors, ThrowOnError> => (options.client ?? client).post<ProjectBuildExportResponses, ProjectBuildExportErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/annotation-exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One image, its revision history, one revision's shapes, and the side of the
 * split its family is on.
 */
export const projectGetImage = <ThrowOnError extends boolean = false>(options: Options<ProjectGetImageData, ThrowOnError>): RequestResult<ProjectGetImageResponses, ProjectGetImageErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetImageResponses, ProjectGetImageErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-image', ...options });

/**
 * The images in a project, newest registration first.
 */
export const projectListImages = <ThrowOnError extends boolean = false>(options: Options<ProjectListImagesData, ThrowOnError>): RequestResult<ProjectListImagesResponses, ProjectListImagesErrors, ThrowOnError> => (options.client ?? client).get<ProjectListImagesResponses, ProjectListImagesErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-images', ...options });

/**
 * Register an image into a project, or correct what is known about it.
 *
 * Re-registering keeps the revisions and the review state: a plan re-submitted
 * with a corrected `group_id` is a correction, and resetting its labels would
 * punish the correction.
 */
export const projectRegisterImage = <ThrowOnError extends boolean = false>(options: Options<ProjectRegisterImageData, ThrowOnError>): RequestResult<ProjectRegisterImageResponses, ProjectRegisterImageErrors, ThrowOnError> => (options.client ?? client).post<ProjectRegisterImageResponses, ProjectRegisterImageErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/annotation-images',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One project, with the counts that answer "is there enough data yet".
 */
export const projectGetProject = <ThrowOnError extends boolean = false>(options: Options<ProjectGetProjectData, ThrowOnError>): RequestResult<ProjectGetProjectResponses, ProjectGetProjectErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetProjectResponses, ProjectGetProjectErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-project', ...options });

/**
 * Every annotation project.
 */
export const projectListProjects = <ThrowOnError extends boolean = false>(options: Options<ProjectListProjectsData, ThrowOnError>): RequestResult<ProjectListProjectsResponses, ProjectListProjectsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListProjectsResponses, ProjectListProjectsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/annotation-projects', ...options });

/**
 * Create a project, or replace its description, split policy and label schema.
 *
 * The schema version is re-derived from the classes. An unchanged class list
 * keeps its version and every accepted revision stays valid; a changed one is
 * a new version, and the next export names every revision drawn under the old
 * one as excluded rather than quietly relabelling them.
 */
export const projectSaveProject = <ThrowOnError extends boolean = false>(options: Options<ProjectSaveProjectData, ThrowOnError>): RequestResult<ProjectSaveProjectResponses, ProjectSaveProjectErrors, ThrowOnError> => (options.client ?? client).post<ProjectSaveProjectResponses, ProjectSaveProjectErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/annotation-projects',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Move an image's review state, and pin the revision an export will read.
 */
export const projectReviewImage = <ThrowOnError extends boolean = false>(options: Options<ProjectReviewImageData, ThrowOnError>): RequestResult<ProjectReviewImageResponses, ProjectReviewImageErrors, ThrowOnError> => (options.client ?? client).post<ProjectReviewImageResponses, ProjectReviewImageErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/annotation-reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Save a drawing.
 *
 * A revision that does not validate against the project's schema is not
 * stored at all, and the 422 carries every problem rather than the first.
 * Storing an invalid revision and marking it so would move the decision about
 * whether a half-drawn door is a training target to whatever reads it next.
 */
export const projectSaveRevision = <ThrowOnError extends boolean = false>(options: Options<ProjectSaveRevisionData, ThrowOnError>): RequestResult<ProjectSaveRevisionResponses, ProjectSaveRevisionErrors, ThrowOnError> => (options.client ?? client).post<ProjectSaveRevisionResponses, ProjectSaveRevisionErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/annotation-revisions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Sessions, each grouping the runs it produced.
 *
 * The level above a run, so the panel can start from "which session" and walk
 * down to a single LLM call without going back to a flat list and re-filtering.
 */
export const projectListConversations = <ThrowOnError extends boolean = false>(options: Options<ProjectListConversationsData, ThrowOnError>): RequestResult<ProjectListConversationsResponses, ProjectListConversationsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListConversationsResponses, ProjectListConversationsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/conversations', ...options });

/**
 * Search reusable block solutions in the selected registry.
 */
export const projectSearchBlockLibrary = <ThrowOnError extends boolean = false>(options: Options<ProjectSearchBlockLibraryData, ThrowOnError>): RequestResult<ProjectSearchBlockLibraryResponses, ProjectSearchBlockLibraryErrors, ThrowOnError> => (options.client ?? client).get<ProjectSearchBlockLibraryResponses, ProjectSearchBlockLibraryErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/curation-library', ...options });

/**
 * Publish a reusable solution through the same registry operation as the seed.
 */
export const projectSaveBlockTemplate = <ThrowOnError extends boolean = false>(options: Options<ProjectSaveBlockTemplateData, ThrowOnError>): RequestResult<ProjectSaveBlockTemplateResponses, ProjectSaveBlockTemplateErrors, ThrowOnError> => (options.client ?? client).post<ProjectSaveBlockTemplateResponses, ProjectSaveBlockTemplateErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/curation-library',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every saved curation pipeline, newest save first.
 */
export const projectListPipelines = <ThrowOnError extends boolean = false>(options: Options<ProjectListPipelinesData, ThrowOnError>): RequestResult<ProjectListPipelinesResponses, ProjectListPipelinesErrors, ThrowOnError> => (options.client ?? client).get<ProjectListPipelinesResponses, ProjectListPipelinesErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/curation-pipelines', ...options });

/**
 * Save a content-addressed revision of a block pipeline.
 *
 * The blocks are checked as a chain here and nowhere else — the canvas draws
 * what it is told and implements no rules of its own, exactly as the
 * annotation canvas does not re-implement the shape validator. A refusal is a
 * 422 whose `details` carry every problem at once.
 */
export const projectSavePipeline = <ThrowOnError extends boolean = false>(options: Options<ProjectSavePipelineData, ThrowOnError>): RequestResult<ProjectSavePipelineResponses, ProjectSavePipelineErrors, ThrowOnError> => (options.client ?? client).post<ProjectSavePipelineResponses, ProjectSavePipelineErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/curation-pipelines',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read an exact saved pipeline, including its layout and notebook pins.
 * A missing revision never falls back to the current head.
 */
export const projectGetPipelineRevision = <ThrowOnError extends boolean = false>(options: Options<ProjectGetPipelineRevisionData, ThrowOnError>): RequestResult<ProjectGetPipelineRevisionResponses, ProjectGetPipelineRevisionErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetPipelineRevisionResponses, ProjectGetPipelineRevisionErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/curation-pipelines/{name}/revisions/{revision}', ...options });

/**
 * Every saved Flow PHP recipe, newest save first.
 */
export const projectListRecipes = <ThrowOnError extends boolean = false>(options: Options<ProjectListRecipesData, ThrowOnError>): RequestResult<ProjectListRecipesResponses, ProjectListRecipesErrors, ThrowOnError> => (options.client ?? client).get<ProjectListRecipesResponses, ProjectListRecipesErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/curations', ...options });

/**
 * Save a content-addressed revision of a Flow PHP recipe.
 */
export const projectSaveRecipe = <ThrowOnError extends boolean = false>(options: Options<ProjectSaveRecipeData, ThrowOnError>): RequestResult<ProjectSaveRecipeResponses, ProjectSaveRecipeErrors, ThrowOnError> => (options.client ?? client).post<ProjectSaveRecipeResponses, ProjectSaveRecipeErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/curations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One immutable dataset version, returned in small slices for an interactive viewer.
 */
export const projectGetDatasetRows = <ThrowOnError extends boolean = false>(options: Options<ProjectGetDatasetRowsData, ThrowOnError>): RequestResult<ProjectGetDatasetRowsResponses, ProjectGetDatasetRowsErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetDatasetRowsResponses, ProjectGetDatasetRowsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/dataset-rows', ...options });

/**
 * Publish explicitly labelled limited output. Sample metadata is required.
 * A separate route prevents older servers from ignoring a new sample field.
 */
export const projectPublishDatasetSample = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishDatasetSampleData, ThrowOnError>): RequestResult<ProjectPublishDatasetSampleResponses, ProjectPublishDatasetSampleErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishDatasetSampleResponses, ProjectPublishDatasetSampleErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/dataset-samples',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every saved dataset, newest execution first.
 */
export const projectListDatasets = <ThrowOnError extends boolean = false>(options: Options<ProjectListDatasetsData, ThrowOnError>): RequestResult<ProjectListDatasetsResponses, ProjectListDatasetsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListDatasetsResponses, ProjectListDatasetsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/datasets', ...options });

/**
 * Persist exact execution output as an immutable version, optionally labelled as a sample.
 */
export const projectPublishDataset = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishDatasetData, ThrowOnError>): RequestResult<ProjectPublishDatasetResponses, ProjectPublishDatasetErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishDatasetResponses, ProjectPublishDatasetErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/datasets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One dimension's rows: the explorer's top level, whatever it is rooted on.
 *
 * `session`, `agent`, `runtime`, `workflow`, `trace`, `model` and `tool` all
 * return the same row shape, so the panel's tree has one renderer rather than
 * one per pivot. See `aiwatcher_projector::dimensions`.
 */
export const projectListDimension = <ThrowOnError extends boolean = false>(options: Options<ProjectListDimensionData, ThrowOnError>): RequestResult<ProjectListDimensionResponses, ProjectListDimensionErrors, ThrowOnError> => (options.client ?? client).get<ProjectListDimensionResponses, ProjectListDimensionErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/dimensions/{kind}', ...options });

/**
 * Every pair this instance has admitted, withdrawn ones included: an approval
 * that vanished from the list would read as one nobody ever made.
 */
export const projectListApprovals = <ThrowOnError extends boolean = false>(options: Options<ProjectListApprovalsData, ThrowOnError>): RequestResult<ProjectListApprovalsResponses, ProjectListApprovalsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListApprovalsResponses, ProjectListApprovalsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-approvals', ...options });

/**
 * Admit one pinned pair.
 *
 * The operator act that authorises publication, separated from publishing so
 * that every later repetition — from a worker, a schedule or CI — needs
 * nothing on the server's host. `Admin`, not `Editor`: an ingest token is an
 * editor by construction, and a producer that can admit its own evidence is
 * not an approval. Idempotent for the pair; a bundle that changed underneath
 * an admitted pair conflicts rather than moving what earlier results mean.
 */
export const projectApproveSource = <ThrowOnError extends boolean = false>(options: Options<ProjectApproveSourceData, ThrowOnError>): RequestResult<ProjectApproveSourceResponses, ProjectApproveSourceErrors, ThrowOnError> => (options.client ?? client).post<ProjectApproveSourceResponses, ProjectApproveSourceErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-approvals',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Withdraw one. Every result measured under that pair stops being readable and
 * no new one can be published; nothing already published has its retention
 * moved, in either direction. Final for this approval ID.
 */
export const projectWithdrawApproval = <ThrowOnError extends boolean = false>(options: Options<ProjectWithdrawApprovalData, ThrowOnError>): RequestResult<ProjectWithdrawApprovalResponses, ProjectWithdrawApprovalErrors, ThrowOnError> => (options.client ?? client).delete<ProjectWithdrawApprovalResponses, ProjectWithdrawApprovalErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-approvals/{approval_id}', ...options });

/**
 * Remove every staged member of one pair.
 *
 * What it does not do is un-admit anything: an approval is withdrawn through
 * its own route, and evidence already published under this pair keeps reading
 * until the adapter is asked for bytes that are no longer there. Use it to
 * correct a bundle before approving, or to clear one that was withdrawn.
 */
export const projectDiscardBundle = <ThrowOnError extends boolean = false>(options: Options<ProjectDiscardBundleData, ThrowOnError>): RequestResult<ProjectDiscardBundleResponses, ProjectDiscardBundleErrors, ThrowOnError> => (options.client ?? client).delete<ProjectDiscardBundleResponses, ProjectDiscardBundleErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-approvals/{approval_id}/bundle', ...options });

/**
 * What is staged for one pair. Names and sizes: the digests are the
 * declaration's, and a second copy of them here could only disagree.
 */
export const projectListBundle = <ThrowOnError extends boolean = false>(options: Options<ProjectListBundleData, ThrowOnError>): RequestResult<ProjectListBundleResponses, ProjectListBundleErrors, ThrowOnError> => (options.client ?? client).get<ProjectListBundleResponses, ProjectListBundleErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-approvals/{approval_id}/bundle', ...options });

/**
 * Stage one member of a bundle, by the name the declaration gives it.
 *
 * This is what lets a *new* pair be admitted with nobody on the server's
 * host. Admin, because approving is: an ingest token is an editor by
 * construction, and bytes that decide what may be published are the operator's
 * act rather than the producer's.
 *
 * It admits nothing on its own. The approval that follows resolves the whole
 * bundle and records its digest, so bytes staged after one do not widen it —
 * they stop that pair reading until they are what was admitted again.
 */
export const projectStageBundle = <ThrowOnError extends boolean = false>(options: Options<ProjectStageBundleData, ThrowOnError>): RequestResult<ProjectStageBundleResponses, ProjectStageBundleErrors, ThrowOnError> => (options.client ?? client).put<ProjectStageBundleResponses, ProjectStageBundleErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-approvals/{approval_id}/bundle/{name}',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * Every standing judgement about one thing, at its current revision.
 */
export const projectListAssessments = <ThrowOnError extends boolean = false>(options: Options<ProjectListAssessmentsData, ThrowOnError>): RequestResult<ProjectListAssessmentsResponses, ProjectListAssessmentsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListAssessmentsResponses, ProjectListAssessmentsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-assessments', ...options });

/**
 * Record one judgement.
 *
 * The caller is who filed it, always: a person's judgement is attributed to
 * the session, and a judge's names the judge while the session still says who
 * ran it. Nothing here checks the target exists — a judgement outlives the
 * trace it is about, which is the whole reason it is written down.
 *
 * Safe to send twice: repeating what the current revision already says lands
 * on that revision rather than recording a second one.
 */
export const projectRecordAssessment = <ThrowOnError extends boolean = false>(options: Options<ProjectRecordAssessmentData, ThrowOnError>): RequestResult<ProjectRecordAssessmentResponses, ProjectRecordAssessmentErrors, ThrowOnError> => (options.client ?? client).post<ProjectRecordAssessmentResponses, ProjectRecordAssessmentErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-assessments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * What one standing judgement said over time, newest first.
 *
 * Both IDs came from the listing. A browser deriving either would be a second
 * answer to what one thing is, which is the rule an approval's address keeps.
 */
export const projectGetAssessmentHistory = <ThrowOnError extends boolean = false>(options: Options<ProjectGetAssessmentHistoryData, ThrowOnError>): RequestResult<ProjectGetAssessmentHistoryResponses, ProjectGetAssessmentHistoryErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetAssessmentHistoryResponses, ProjectGetAssessmentHistoryErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-assessments/{target_id}/{standing_id}', ...options });

/**
 * Freeze what people judged of one result, as a judge's calibration set.
 *
 * Only a person's judgement, and only under the rubric versions named: a
 * judgement made under other words answered another question. Addressed by
 * its content, so taking the same judgements twice is the same set — and a
 * person changing their mind later is a later set, never this one re-read.
 */
export const projectTakeCalibration = <ThrowOnError extends boolean = false>(options: Options<ProjectTakeCalibrationData, ThrowOnError>): RequestResult<ProjectTakeCalibrationResponses, ProjectTakeCalibrationErrors, ThrowOnError> => (options.client ?? client).post<ProjectTakeCalibrationResponses, ProjectTakeCalibrationErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-calibrations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One calibration set, at the address a declaration names it by.
 */
export const projectGetCalibration = <ThrowOnError extends boolean = false>(options: Options<ProjectGetCalibrationData, ThrowOnError>): RequestResult<ProjectGetCalibrationResponses, ProjectGetCalibrationErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetCalibrationResponses, ProjectGetCalibrationErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-calibrations/{version}', ...options });

/**
 * Take a cohort from a dataset version this deployment owns.
 *
 * The first `limit` cases of a version's split, as the owner holds them, with
 * the three files a cohort pins derived here — so a declaration names them
 * and nobody writes or stages them. Admitting the pair derives them again from
 * the owner. A conversation corpus's cases are content, so an admin takes a
 * cohort from one, as an admin reads its cases. Project routes currently support
 * curation versions and annotation exports only; conversations remain unavailable.
 */
export const projectDeriveCohort = <ThrowOnError extends boolean = false>(options: Options<ProjectDeriveCohortData, ThrowOnError>): RequestResult<ProjectDeriveCohortResponses, ProjectDeriveCohortErrors, ThrowOnError> => (options.client ?? client).post<ProjectDeriveCohortResponses, ProjectDeriveCohortErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-cohorts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Where the cases under one digest were derived from, if this deployment
 * derived them.
 */
export const projectGetDerivedCohort = <ThrowOnError extends boolean = false>(options: Options<ProjectGetDerivedCohortData, ThrowOnError>): RequestResult<ProjectGetDerivedCohortResponses, ProjectGetDerivedCohortErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetDerivedCohortResponses, ProjectGetDerivedCohortErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-cohorts/{cases}', ...options });

/**
 * Download a staged answer document after verifying its SHA-256 digest.
 * Returns the original JSON bytes, preserving number spelling and whitespace.
 */
export const projectGetRecording = <ThrowOnError extends boolean = false>(options: Options<ProjectGetRecordingData, ThrowOnError>): RequestResult<ProjectGetRecordingResponses, ProjectGetRecordingErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetRecordingResponses, ProjectGetRecordingErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-recordings/{digest}/content', ...options });

/**
 * Keep answers and return a reference to their exact bytes. The name is display
 * metadata, never a storage key. Retrying the same bytes preserves their digest.
 * On project routes the reference is interpreted only in that same project.
 */
export const projectStageRecording = <ThrowOnError extends boolean = false>(options: Options<ProjectStageRecordingData, ThrowOnError>): RequestResult<ProjectStageRecordingResponses, ProjectStageRecordingErrors, ThrowOnError> => (options.client ?? client).put<ProjectStageRecordingResponses, ProjectStageRecordingErrors, ThrowOnError>({
    bodySerializer: null,
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-recordings/{name}',
    ...options,
    headers: {
        'Content-Type': 'application/octet-stream',
        ...options.headers
    }
});

/**
 * Durable discovery is independent of telemetry retention. Newest first, and
 * clients page with the opaque cursor rather than a timestamp assumption.
 * Conversation content requires Admin; other readers receive a forbidden state.
 */
export const projectListResults = <ThrowOnError extends boolean = false>(options: Options<ProjectListResultsData, ThrowOnError>): RequestResult<ProjectListResultsResponses, ProjectListResultsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListResultsResponses, ProjectListResultsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-results', ...options });

/**
 * Publish terminal evidence. Retry the identical body and logical ID after a
 * timeout; a different body conflicts. No result content enters the event log.
 * Conversation evidence requires Admin, including publication.
 *
 * A pair nobody has admitted yet is a 409 `pair_not_admitted` naming the
 * approval, as starting a scoring run is; a withdrawn pair stays a 403.
 */
export const projectPublishResult = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishResultData, ThrowOnError>): RequestResult<ProjectPublishResultResponses, ProjectPublishResultErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishResultResponses, ProjectPublishResultErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-results',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Forget one published result.
 *
 * The third way evidence disappears, beside its approval being withdrawn and
 * its retention running out — and the only one that is about a single
 * measurement. It writes the same durable marker the retention sweep does, so
 * the ID stays permanently unreadable rather than falling back to telemetry.
 */
export const projectForgetResult = <ThrowOnError extends boolean = false>(options: Options<ProjectForgetResultData, ThrowOnError>): RequestResult<ProjectForgetResultResponses, ProjectForgetResultErrors, ThrowOnError> => (options.client ?? client).delete<ProjectForgetResultResponses, ProjectForgetResultErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-results/{evaluation_id}', ...options });

export const projectGetResult = <ThrowOnError extends boolean = false>(options: Options<ProjectGetResultData, ThrowOnError>): RequestResult<ProjectGetResultResponses, ProjectGetResultErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetResultResponses, ProjectGetResultErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-results/{evaluation_id}', ...options });

export const projectGetCases = <ThrowOnError extends boolean = false>(options: Options<ProjectGetCasesData, ThrowOnError>): RequestResult<ProjectGetCasesResponses, ProjectGetCasesErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetCasesResponses, ProjectGetCasesErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-results/{evaluation_id}/cases', ...options });

/**
 * One published result against another named one.
 *
 * Two headers, not two results: the metrics a comparison subtracts are in the
 * metadata each side was published with, so this costs what two summaries
 * cost however many cases are behind them. Which two may be subtracted is the
 * server's answer — `comparability` with its reasons, the same three words the
 * folded half uses — and a delta is withheld rather than shown wrong. Case
 * level regressions are not here: they are a full read of both sides.
 */
export const projectCompareResults = <ThrowOnError extends boolean = false>(options: Options<ProjectCompareResultsData, ThrowOnError>): RequestResult<ProjectCompareResultsResponses, ProjectCompareResultsErrors, ThrowOnError> => (options.client ?? client).get<ProjectCompareResultsResponses, ProjectCompareResultsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-results/{evaluation_id}/comparison', ...options });

/**
 * Which cases moved between one published result and another named one.
 *
 * The comparison beside this one reads two headers; this reads both results,
 * so it pages, it is narrowed here rather than in the browser, and one
 * request walks a bounded number of cases. The verdict rides along, so a pair
 * this server will not subtract is refused in the same sentence the header
 * comparison uses rather than as an empty list — and an unverified pair keeps
 * its rows, because the cases that failed are what somebody opens this for.
 */
export const projectCompareCases = <ThrowOnError extends boolean = false>(options: Options<ProjectCompareCasesData, ThrowOnError>): RequestResult<ProjectCompareCasesResponses, ProjectCompareCasesErrors, ThrowOnError> => (options.client ?? client).get<ProjectCompareCasesResponses, ProjectCompareCasesErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-results/{evaluation_id}/comparison/cases', ...options });

/**
 * Every proposal for one dataset, oldest first, at its current revision.
 */
export const projectListReviews = <ThrowOnError extends boolean = false>(options: Options<ProjectListReviewsData, ThrowOnError>): RequestResult<ProjectListReviewsResponses, ProjectListReviewsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListReviewsResponses, ProjectListReviewsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-reviews', ...options });

/**
 * Propose a case. Proposing what was noticed on the same target for the same
 * dataset again answers the review already under way, 200 rather than 201.
 *
 * Without a question, a case target is read at `at` — the position a
 * comparison row carries — for what the cohort asked and what the variant
 * answered, and the proposal is `measured`. A trace holds no words to read.
 * Project routes currently require supplied `written` or `observed` words;
 * result-source resolution remains unavailable in that scope.
 */
export const projectProposeCase = <ThrowOnError extends boolean = false>(options: Options<ProjectProposeCaseData, ThrowOnError>): RequestResult<ProjectProposeCaseResponses, ProjectProposeCaseErrors, ThrowOnError> => (options.client ?? client).post<ProjectProposeCaseResponses, ProjectProposeCaseErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every proposal seen on one target — a trace, a span, a session or a case
 * of a result — whichever dataset each would join, at its current revision.
 * What a case's judgements read to say the case is already under review.
 */
export const projectReviewsOfTarget = <ThrowOnError extends boolean = false>(options: Options<ProjectReviewsOfTargetData, ThrowOnError>): RequestResult<ProjectReviewsOfTargetResponses, ProjectReviewsOfTargetErrors, ThrowOnError> => (options.client ?? client).get<ProjectReviewsOfTargetResponses, ProjectReviewsOfTargetErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-reviews/of-target', ...options });

/**
 * Publish every approved case as a new version of its dataset: the current
 * version's rows, then one row per case, `review-…` by its ID.
 *
 * A dataset whose rows are not cases — `case_id`, `input`, `expected` — is
 * refused rather than given rows of a second shape. Publishing the same
 * approved set again lands on the same version.
 */
export const projectPublishReviews = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishReviewsData, ThrowOnError>): RequestResult<ProjectPublishReviewsResponses, ProjectPublishReviewsErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishReviewsResponses, ProjectPublishReviewsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-reviews/publish', ...options });

/**
 * Write an expected answer, approve or reject. Approving words somebody using
 * the application said — `observed` content — takes the admin role of the
 * project on project routes, or the instance on legacy routes.
 */
export const projectReviewCase = <ThrowOnError extends boolean = false>(options: Options<ProjectReviewCaseData, ThrowOnError>): RequestResult<ProjectReviewCaseResponses, ProjectReviewCaseErrors, ThrowOnError> => (options.client ?? client).post<ProjectReviewCaseResponses, ProjectReviewCaseErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-reviews/{id}/actions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every form this instance holds, at its current version.
 */
export const projectListRubrics = <ThrowOnError extends boolean = false>(options: Options<ProjectListRubricsData, ThrowOnError>): RequestResult<ProjectListRubricsResponses, ProjectListRubricsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListRubricsResponses, ProjectListRubricsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-rubrics', ...options });

/**
 * Publish a form. Idempotent by content: the same words answer with the
 * version that is already there, and the head moves to it either way.
 */
export const projectPublishRubric = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishRubricData, ThrowOnError>): RequestResult<ProjectPublishRubricResponses, ProjectPublishRubricErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishRubricResponses, ProjectPublishRubricErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-rubrics',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One form, at the version asked for or at the head.
 */
export const projectGetRubric = <ThrowOnError extends boolean = false>(options: Options<ProjectGetRubricData, ThrowOnError>): RequestResult<ProjectGetRubricResponses, ProjectGetRubricErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetRubricResponses, ProjectGetRubricErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-rubrics/{name}', ...options });

/**
 * Declare a measurement.
 *
 * Writes nothing that runs. What comes back is the declaration with the
 * manifest a result of it publishes and the approval that admits that pair,
 * because an operator has to admit it before it may publish and the metrics
 * in that manifest are derived from the card — a second copy written out by
 * hand is a second answer to what this run measures. Idempotent by content.
 *
 * The card is resolved here rather than at score time, so a version nobody
 * published is a refusal now instead of a run that fails in a minute.
 * Project declarations currently support saved recordings over native cohorts
 * with built-in scorers only. They neither admit nor start a measurement.
 */
export const projectDeclareScoringRun = <ThrowOnError extends boolean = false>(options: Options<ProjectDeclareScoringRunData, ThrowOnError>): RequestResult<ProjectDeclareScoringRunResponses, ProjectDeclareScoringRunErrors, ThrowOnError> => (options.client ?? client).post<ProjectDeclareScoringRunResponses, ProjectDeclareScoringRunErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-runs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * What a run measures, what it publishes, and whether it may yet.
 */
export const projectGetScoringRun = <ThrowOnError extends boolean = false>(options: Options<ProjectGetScoringRunData, ThrowOnError>): RequestResult<ProjectGetScoringRunResponses, ProjectGetScoringRunErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetScoringRunResponses, ProjectGetScoringRunErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-runs/{id}', ...options });

/**
 * Start the declared measurement.
 *
 * Refused while nothing admits the pair, naming the approval that would. A
 * run started without one could only fail when it tried to publish, and that
 * failed run would then be what every later start of the same declaration
 * lands on. Once admitted, repeating this reaches the run already going.
 */
export const projectStartScoringRun = <ThrowOnError extends boolean = false>(options: Options<ProjectStartScoringRunData, ThrowOnError>): RequestResult<ProjectStartScoringRunResponses, ProjectStartScoringRunErrors, ThrowOnError> => (options.client ?? client).post<ProjectStartScoringRunResponses, ProjectStartScoringRunErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-runs/{id}/start', ...options });

/**
 * Every scorecard this instance holds, at its current version.
 *
 * A head carries the metrics its version declares, so a form choosing what to
 * run says what each card would measure without opening it.
 */
export const projectListScorecards = <ThrowOnError extends boolean = false>(options: Options<ProjectListScorecardsData, ThrowOnError>): RequestResult<ProjectListScorecardsResponses, ProjectListScorecardsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListScorecardsResponses, ProjectListScorecardsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-scorecards', ...options });

/**
 * Publish a scorecard. Idempotent by content: the same measurements answer
 * with the version that is already there, and the head moves to it either
 * way.
 *
 * Nothing here is code. A scorer is a name this deployment implements and the
 * parameters that name takes, so publishing a card is not a way to run
 * something on the server's host.
 */
export const projectPublishScorecard = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishScorecardData, ThrowOnError>): RequestResult<ProjectPublishScorecardResponses, ProjectPublishScorecardErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishScorecardResponses, ProjectPublishScorecardErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-scorecards',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One scorecard, at the version asked for or at the head.
 */
export const projectGetScorecard = <ThrowOnError extends boolean = false>(options: Options<ProjectGetScorecardData, ThrowOnError>): RequestResult<ProjectGetScorecardResponses, ProjectGetScorecardErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetScorecardResponses, ProjectGetScorecardErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-scorecards/{name}', ...options });

/**
 * What changed from one version of a card to another: the metrics added,
 * removed and changed, each changed field of a scorer by JSON pointer, and
 * what each metric was derived to be on both sides — the part a version does
 * not hold.
 */
export const projectDiffScorecard = <ThrowOnError extends boolean = false>(options: Options<ProjectDiffScorecardData, ThrowOnError>): RequestResult<ProjectDiffScorecardResponses, ProjectDiffScorecardErrors, ThrowOnError> => (options.client ?? client).get<ProjectDiffScorecardResponses, ProjectDiffScorecardErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-scorecards/{name}/diff', ...options });

/**
 * Every version of one card, newest first — each whole, so a form can start
 * from any of them and a reader can pick two to compare.
 */
export const projectListScorecardVersions = <ThrowOnError extends boolean = false>(options: Options<ProjectListScorecardVersionsData, ThrowOnError>): RequestResult<ProjectListScorecardVersionsResponses, ProjectListScorecardVersionsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListScorecardVersionsResponses, ProjectListScorecardVersionsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-scorecards/{name}/versions', ...options });

/**
 * Suites: the level above a report, and what MLflow calls an experiment.
 *
 * A separate resource rather than `/evaluations/suites`, so an evaluation
 * that happens to be called `suites` is still reachable.
 */
export const projectListEvaluationSuites = <ThrowOnError extends boolean = false>(options: Options<ProjectListEvaluationSuitesData, ThrowOnError>): RequestResult<ProjectListEvaluationSuitesResponses, ProjectListEvaluationSuitesErrors, ThrowOnError> => (options.client ?? client).get<ProjectListEvaluationSuitesResponses, ProjectListEvaluationSuitesErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluation-suites', ...options });

/**
 * Evaluation reports, newest first.
 *
 * The other half of the loop the traces come from: a trace says what one run
 * did, an evaluation says whether the thing producing those runs is getting
 * better. They arrive on the same log and are folded apart — an `eval.*`
 * event produces no span and no row in the runs list. See
 * `aiwatcher_projector::evaluations`.
 */
export const projectListEvaluations = <ThrowOnError extends boolean = false>(options: Options<ProjectListEvaluationsData, ThrowOnError>): RequestResult<ProjectListEvaluationsResponses, ProjectListEvaluationsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListEvaluationsResponses, ProjectListEvaluationsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluations', ...options });

/**
 * One evaluation: its parameters, its metrics, its cases, the document the
 * producer attached, and the previous evaluation of the same suite on the
 * same dataset.
 */
export const projectGetEvaluation = <ThrowOnError extends boolean = false>(options: Options<ProjectGetEvaluationData, ThrowOnError>): RequestResult<ProjectGetEvaluationResponses, ProjectGetEvaluationErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetEvaluationResponses, ProjectGetEvaluationErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/evaluations/{evaluation_id}', ...options });

/**
 * Server-sent events for the whole system: a catch-up marker, then live.
 *
 * This is the panel's Observability transport. It is deliberately separate
 * from the WebSocket below: the panel only receives here, so EventSource can
 * own reconnects and `Last-Event-ID` resume without client-side machinery.
 */
export const projectStreamEvents = <ThrowOnError extends boolean = false>(options: Options<ProjectStreamEventsData, ThrowOnError>): RequestResult<ProjectStreamEventsResponses, ProjectStreamEventsErrors, ThrowOnError> => (options.client ?? client).get<ProjectStreamEventsResponses, ProjectStreamEventsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/events/stream', ...options });

/**
 * The contexts results were published in, newest first — each an experiment:
 * what it measures, how many results and how many variants.
 */
export const projectListExperiments = <ThrowOnError extends boolean = false>(options: Options<ProjectListExperimentsData, ThrowOnError>): RequestResult<ProjectListExperimentsResponses, ProjectListExperimentsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListExperimentsResponses, ProjectListExperimentsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/experiments', ...options });

/**
 * Every result published in one context, newest first, compared with the
 * baseline when one is named — and the whole-run timing of each managed run
 * behind them.
 */
export const projectGetExperiment = <ThrowOnError extends boolean = false>(options: Options<ProjectGetExperimentData, ThrowOnError>): RequestResult<ProjectGetExperimentResponses, ProjectGetExperimentErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetExperimentResponses, ProjectGetExperimentErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/experiments/{context_id}', ...options });

/**
 * Every lab in this workshop, by position and then by name.
 */
export const projectListLabs = <ThrowOnError extends boolean = false>(options: Options<ProjectListLabsData, ThrowOnError>): RequestResult<ProjectListLabsResponses, ProjectListLabsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListLabsResponses, ProjectListLabsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/labs', ...options });

/**
 * Publish a version of a lab.
 *
 * Idempotent on the document: `created` is `false` when this exact lab was
 * already stored, and the version that comes back is the one that was there.
 *
 * A lab that pins tests is refused unless both halves resolve in this
 * project's evaluation registry — a stored lab that measures nothing is a
 * slot that reads as working and answers nothing when somebody submits.
 */
export const projectPublishLab = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishLabData, ThrowOnError>): RequestResult<ProjectPublishLabResponses, ProjectPublishLabErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishLabResponses, ProjectPublishLabErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/labs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One lab: its versions, its labels, and the brief it is at now.
 */
export const projectGetLab = <ThrowOnError extends boolean = false>(options: Options<ProjectGetLabData, ThrowOnError>): RequestResult<ProjectGetLabResponses, ProjectGetLabErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetLabResponses, ProjectGetLabErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/labs/{name}', ...options });

/**
 * Move a label onto a version that is already stored.
 *
 * Its own route because writing a lesson and setting it are different
 * decisions — `POST /labs` with a `label` is the shorthand for doing both at
 * once, which is what a first publish wants.
 */
export const projectSetLabLabel = <ThrowOnError extends boolean = false>(options: Options<ProjectSetLabLabelData, ThrowOnError>): RequestResult<ProjectSetLabLabelResponses, ProjectSetLabLabelErrors, ThrowOnError> => (options.client ?? client).put<ProjectSetLabLabelResponses, ProjectSetLabLabelErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/labs/{name}/labels/{label}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The context every submission to this lab publishes under.
 *
 * `context_id` is what `GET /api/v1/evaluation-results?context_id=…` is
 * narrowed by, so this is the whole join from a lab to its marks. It is
 * answerable before anybody has submitted, which is the point.
 */
export const projectGetLabMeasurement = <ThrowOnError extends boolean = false>(options: Options<ProjectGetLabMeasurementData, ThrowOnError>): RequestResult<ProjectGetLabMeasurementResponses, ProjectGetLabMeasurementErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetLabMeasurementResponses, ProjectGetLabMeasurementErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/labs/{name}/measurement', ...options });

/**
 * One version, with its brief.
 */
export const projectGetLabVersion = <ThrowOnError extends boolean = false>(options: Options<ProjectGetLabVersionData, ThrowOnError>): RequestResult<ProjectGetLabVersionResponses, ProjectGetLabVersionErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetLabVersionResponses, ProjectGetLabVersionErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/labs/{name}/versions/{version_id}', ...options });

/**
 * A WebSocket for the whole system, or one run.
 *
 * Prefer SSE where the traffic is one-way — it reconnects on its own and
 * survives proxies better. Use this when the panel needs to send *up* as well:
 * cancelling a run, approving a tool call, submitting human feedback.
 */
export const projectLiveWebsocket = <ThrowOnError extends boolean = false>(options: Options<ProjectLiveWebsocketData, ThrowOnError>): RequestResult<unknown, ProjectLiveWebsocketErrors, ThrowOnError> => (options.client ?? client).get<unknown, ProjectLiveWebsocketErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/live', ...options });

/**
 * Aggregates over the runs the projector still holds.
 *
 * Served from the read model rather than from a metrics backend: the numbers
 * are a fold over data already in memory, so the page renders with no PromQL
 * and no dependency on VictoriaMetrics being reachable. The window is bounded
 * by retention — `window.runs_retained` against `window.retention_limit` tells
 * a caller whether it is looking at everything or at a truncated tail.
 */
export const projectGetMetrics = <ThrowOnError extends boolean = false>(options: Options<ProjectGetMetricsData, ThrowOnError>): RequestResult<ProjectGetMetricsResponses, ProjectGetMetricsErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetMetricsResponses, ProjectGetMetricsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/metrics', ...options });

/**
 * Every registered model.
 */
export const projectListModels = <ThrowOnError extends boolean = false>(options: Options<ProjectListModelsData, ThrowOnError>): RequestResult<ProjectListModelsResponses, ProjectListModelsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListModelsResponses, ProjectListModelsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/models', ...options });

/**
 * Register what a run produced.
 *
 * The provenance — dataset, framework, code — is read from the run rather
 * than from this request, so a version cannot claim a lineage the run it
 * names does not have. A version that cannot be promoted is still recorded,
 * and the reason comes back with it.
 */
export const projectRegisterModel = <ThrowOnError extends boolean = false>(options: Options<ProjectRegisterModelData, ThrowOnError>): RequestResult<ProjectRegisterModelResponses, ProjectRegisterModelErrors, ThrowOnError> => (options.client ?? client).post<ProjectRegisterModelResponses, ProjectRegisterModelErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/models',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One model: its versions, its labels, and one version's record.
 */
export const projectGetModel = <ThrowOnError extends boolean = false>(options: Options<ProjectGetModelData, ThrowOnError>): RequestResult<ProjectGetModelResponses, ProjectGetModelErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetModelResponses, ProjectGetModelErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/models/{name}', ...options });

/**
 * Point a label at a version.
 *
 * The one write here that changes what a service loads next, so it needs
 * instance `admin` on legacy routes or project `admin` on scoped routes
 * — and the one the registry itself can refuse: a version with no
 * held-out measurement, or one trained on a dataset name nobody can
 * reconstruct, is not promotable however much anybody wants it to be.
 */
export const projectSetModelLabel = <ThrowOnError extends boolean = false>(options: Options<ProjectSetModelLabelData, ThrowOnError>): RequestResult<ProjectSetModelLabelResponses, ProjectSetModelLabelErrors, ThrowOnError> => (options.client ?? client).post<ProjectSetModelLabelResponses, ProjectSetModelLabelErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/models/{name}/labels',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Every prompt in the registry.
 */
export const projectListPrompts = <ThrowOnError extends boolean = false>(options: Options<ProjectListPromptsData, ThrowOnError>): RequestResult<ProjectListPromptsResponses, ProjectListPromptsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListPromptsResponses, ProjectListPromptsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/prompts', ...options });

/**
 * Publish a version.
 *
 * Idempotent on the text: `created` is `false` when this exact prompt was
 * already stored, and the version that comes back is the one that was there,
 * with its original author and notes intact.
 */
export const projectPublishPrompt = <ThrowOnError extends boolean = false>(options: Options<ProjectPublishPromptData, ThrowOnError>): RequestResult<ProjectPublishPromptResponses, ProjectPublishPromptErrors, ThrowOnError> => (options.client ?? client).post<ProjectPublishPromptResponses, ProjectPublishPromptErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/prompts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One prompt: its labels, its versions, and what happened to it lately.
 */
export const projectGetPrompt = <ThrowOnError extends boolean = false>(options: Options<ProjectGetPromptData, ThrowOnError>): RequestResult<ProjectGetPromptResponses, ProjectGetPromptErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetPromptResponses, ProjectGetPromptErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/prompts/{name}', ...options });

/**
 * Point a label at a version. `production` is the one an SDK reads.
 *
 * A `PUT`, because moving a label twice to the same version is the same
 * world as moving it once.
 */
export const projectSetPromptLabel = <ThrowOnError extends boolean = false>(options: Options<ProjectSetPromptLabelData, ThrowOnError>): RequestResult<ProjectSetPromptLabelResponses, ProjectSetPromptLabelErrors, ThrowOnError> => (options.client ?? client).put<ProjectSetPromptLabelResponses, ProjectSetPromptLabelErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/prompts/{name}/labels/{label}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Record an optimisation and store its candidate.
 *
 * The verdict in the response is computed here from the held-out scores and
 * from what the candidate did to the baseline's variables — it is not read
 * from the request. An optimiser selected its candidate by maximising the
 * number it is reporting, which makes it the last thing that should grade it.
 */
export const projectRecordOptimization = <ThrowOnError extends boolean = false>(options: Options<ProjectRecordOptimizationData, ThrowOnError>): RequestResult<ProjectRecordOptimizationResponses, ProjectRecordOptimizationErrors, ThrowOnError> => (options.client ?? client).post<ProjectRecordOptimizationResponses, ProjectRecordOptimizationErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/prompts/{name}/optimizations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One optimisation, with the report the optimiser attached.
 */
export const projectGetOptimization = <ThrowOnError extends boolean = false>(options: Options<ProjectGetOptimizationData, ThrowOnError>): RequestResult<ProjectGetOptimizationResponses, ProjectGetOptimizationErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetOptimizationResponses, ProjectGetOptimizationErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/prompts/{name}/optimizations/{optimization_id}', ...options });

/**
 * Re-derive the prompt's index from the objects that are stored.
 *
 * The repair path, exposed because the thing it repairs — a head that lost a
 * concurrent write — is invisible until somebody notices a version missing
 * from a list. Labels survive it; a label pointing at a version that is gone
 * does not.
 */
export const projectRebuildPrompt = <ThrowOnError extends boolean = false>(options: Options<ProjectRebuildPromptData, ThrowOnError>): RequestResult<ProjectRebuildPromptResponses, ProjectRebuildPromptErrors, ThrowOnError> => (options.client ?? client).post<ProjectRebuildPromptResponses, ProjectRebuildPromptErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/prompts/{name}/rebuild', ...options });

/**
 * One version, with its text.
 */
export const projectGetPromptVersion = <ThrowOnError extends boolean = false>(options: Options<ProjectGetPromptVersionData, ThrowOnError>): RequestResult<ProjectGetPromptVersionResponses, ProjectGetPromptVersionErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetPromptVersionResponses, ProjectGetPromptVersionErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/prompts/{name}/versions/{version_id}', ...options });

/**
 * List runs, newest first.
 */
export const projectListRuns = <ThrowOnError extends boolean = false>(options: Options<ProjectListRunsData, ThrowOnError>): RequestResult<ProjectListRunsResponses, ProjectListRunsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListRunsResponses, ProjectListRunsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/runs', ...options });

/**
 * One run with the spans finished so far.
 */
export const projectGetRun = <ThrowOnError extends boolean = false>(options: Options<ProjectGetRunData, ThrowOnError>): RequestResult<ProjectGetRunResponses, ProjectGetRunErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetRunResponses, ProjectGetRunErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/runs/{run_id}', ...options });

/**
 * Read from the durable log rather than the read model: this is the audit
 * view, and it must show what was actually recorded.
 *
 * Which side it is on is read from the log too — from the run's **first**
 * event, which is the same rule the runs fold keeps (`RunSummary::project`,
 * first-seen-wins). Asking the read model instead would have left a hole with
 * a name: a project's run whose row had been evicted would become readable on
 * the global side, on the one route that answers past the read model.
 */
export const projectGetRunEvents = <ThrowOnError extends boolean = false>(options: Options<ProjectGetRunEventsData, ThrowOnError>): RequestResult<ProjectGetRunEventsResponses, ProjectGetRunEventsErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetRunEventsResponses, ProjectGetRunEventsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/runs/{run_id}/events', ...options });

/**
 * Server-sent events for one run: history, a catch-up marker, then live.
 */
export const projectStreamRun = <ThrowOnError extends boolean = false>(options: Options<ProjectStreamRunData, ThrowOnError>): RequestResult<ProjectStreamRunResponses, ProjectStreamRunErrors, ThrowOnError> => (options.client ?? client).get<ProjectStreamRunResponses, ProjectStreamRunErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/runs/{run_id}/stream', ...options });

/**
 * Every retained span, flat and filterable.
 *
 * The waterfall answers "show me this run". This answers "show me every tool
 * call slower than two seconds", which is the question asked when the run to
 * look at is not yet known.
 */
export const projectListSpans = <ThrowOnError extends boolean = false>(options: Options<ProjectListSpansData, ThrowOnError>): RequestResult<ProjectListSpansResponses, ProjectListSpansErrors, ThrowOnError> => (options.client ?? client).get<ProjectListSpansResponses, ProjectListSpansErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/spans', ...options });

/**
 * Training runs, newest first.
 */
export const projectListTrainingRuns = <ThrowOnError extends boolean = false>(options: Options<ProjectListTrainingRunsData, ThrowOnError>): RequestResult<ProjectListTrainingRunsResponses, ProjectListTrainingRunsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListTrainingRunsResponses, ProjectListTrainingRunsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/training-runs', ...options });

/**
 * Open a training run.
 *
 * Answered before the first epoch on purpose: if this instance is going to
 * refuse the run, a trainer should find out now rather than after six GPU
 * hours. Re-opening an already-open run returns it, so a retried start does
 * not lose the curve it already wrote.
 */
export const projectStartTrainingRun = <ThrowOnError extends boolean = false>(options: Options<ProjectStartTrainingRunData, ThrowOnError>): RequestResult<ProjectStartTrainingRunResponses, ProjectStartTrainingRunErrors, ThrowOnError> => (options.client ?? client).post<ProjectStartTrainingRunResponses, ProjectStartTrainingRunErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/training-runs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One run, with its whole curve.
 */
export const projectGetTrainingRun = <ThrowOnError extends boolean = false>(options: Options<ProjectGetTrainingRunData, ThrowOnError>): RequestResult<ProjectGetTrainingRunResponses, ProjectGetTrainingRunErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetTrainingRunResponses, ProjectGetTrainingRunErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/training-runs/{run_id}', ...options });

/**
 * Close a run.
 */
export const projectFinishTrainingRun = <ThrowOnError extends boolean = false>(options: Options<ProjectFinishTrainingRunData, ThrowOnError>): RequestResult<ProjectFinishTrainingRunResponses, ProjectFinishTrainingRunErrors, ThrowOnError> => (options.client ?? client).post<ProjectFinishTrainingRunResponses, ProjectFinishTrainingRunErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/training-runs/{run_id}/finish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One batch of progress: epochs, sampled points, checkpoints, profiles.
 *
 * Returns the summary rather than the whole record, because a trainer flushing
 * every epoch does not want its own curve back every time.
 */
export const projectRecordTrainingProgress = <ThrowOnError extends boolean = false>(options: Options<ProjectRecordTrainingProgressData, ThrowOnError>): RequestResult<ProjectRecordTrainingProgressResponses, ProjectRecordTrainingProgressErrors, ThrowOnError> => (options.client ?? client).post<ProjectRecordTrainingProgressResponses, ProjectRecordTrainingProgressErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/training-runs/{run_id}/progress',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const projectListWorkflowDefinitions = <ThrowOnError extends boolean = false>(options: Options<ProjectListWorkflowDefinitionsData, ThrowOnError>): RequestResult<ProjectListWorkflowDefinitionsResponses, ProjectListWorkflowDefinitionsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListWorkflowDefinitionsResponses, ProjectListWorkflowDefinitionsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/workflow-definitions', ...options });

export const projectRegisterWorkflow = <ThrowOnError extends boolean = false>(options: Options<ProjectRegisterWorkflowData, ThrowOnError>): RequestResult<ProjectRegisterWorkflowResponses, ProjectRegisterWorkflowErrors, ThrowOnError> => (options.client ?? client).post<ProjectRegisterWorkflowResponses, ProjectRegisterWorkflowErrors, ThrowOnError>({
    url: '/api/v1/orgs/{organization}/projects/{project}/workflow-definitions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const projectGetWorkflowDefinition = <ThrowOnError extends boolean = false>(options: Options<ProjectGetWorkflowDefinitionData, ThrowOnError>): RequestResult<ProjectGetWorkflowDefinitionResponses, ProjectGetWorkflowDefinitionErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetWorkflowDefinitionResponses, ProjectGetWorkflowDefinitionErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/workflow-definitions/{name}', ...options });

/**
 * Executions, newest first.
 */
export const projectListWorkflowExecutions = <ThrowOnError extends boolean = false>(options: Options<ProjectListWorkflowExecutionsData, ThrowOnError>): RequestResult<ProjectListWorkflowExecutionsResponses, ProjectListWorkflowExecutionsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListWorkflowExecutionsResponses, ProjectListWorkflowExecutionsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/workflow-executions', ...options });

/**
 * One execution: its nodes, its declared edges, and the messages observed.
 */
export const projectGetWorkflowExecution = <ThrowOnError extends boolean = false>(options: Options<ProjectGetWorkflowExecutionData, ThrowOnError>): RequestResult<ProjectGetWorkflowExecutionResponses, ProjectGetWorkflowExecutionErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetWorkflowExecutionResponses, ProjectGetWorkflowExecutionErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/workflow-executions/{workflow_run_id}', ...options });

/**
 * Server-sent events for one execution: history, a catch-up marker, then live.
 *
 * Scoped by `workflow_run_id` rather than by run, which is what makes a
 * stage-per-pod workflow watchable: the pod that has not started yet is the
 * interesting one, and a subscription resolved to today's run ids would never
 * see it.
 */
export const projectStreamWorkflowExecution = <ThrowOnError extends boolean = false>(options: Options<ProjectStreamWorkflowExecutionData, ThrowOnError>): RequestResult<ProjectStreamWorkflowExecutionResponses, ProjectStreamWorkflowExecutionErrors, ThrowOnError> => (options.client ?? client).get<ProjectStreamWorkflowExecutionResponses, ProjectStreamWorkflowExecutionErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/workflow-executions/{workflow_run_id}/stream', ...options });

/**
 * Every workflow the projection knows, most recently active first.
 *
 * Includes workflows nothing ever declared: a producer that has been setting
 * `workflow_id` since before there was a graph to draw still belongs in the
 * picker, with whatever shape its runs revealed.
 */
export const projectListWorkflows = <ThrowOnError extends boolean = false>(options: Options<ProjectListWorkflowsData, ThrowOnError>): RequestResult<ProjectListWorkflowsResponses, ProjectListWorkflowsErrors, ThrowOnError> => (options.client ?? client).get<ProjectListWorkflowsResponses, ProjectListWorkflowsErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/workflows', ...options });

/**
 * One workflow's declared shape.
 */
export const projectGetWorkflow = <ThrowOnError extends boolean = false>(options: Options<ProjectGetWorkflowData, ThrowOnError>): RequestResult<ProjectGetWorkflowResponses, ProjectGetWorkflowErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetWorkflowResponses, ProjectGetWorkflowErrors, ThrowOnError>({ url: '/api/v1/orgs/{organization}/projects/{project}/workflows/{workflow_id}', ...options });

/**
 * Every prompt in the registry.
 */
export const listPrompts = <ThrowOnError extends boolean = false>(options?: Options<ListPromptsData, ThrowOnError>): RequestResult<ListPromptsResponses, ListPromptsErrors, ThrowOnError> => (options?.client ?? client).get<ListPromptsResponses, ListPromptsErrors, ThrowOnError>({ url: '/api/v1/prompts', ...options });

/**
 * Publish a version.
 *
 * Idempotent on the text: `created` is `false` when this exact prompt was
 * already stored, and the version that comes back is the one that was there,
 * with its original author and notes intact.
 */
export const publishPrompt = <ThrowOnError extends boolean = false>(options: Options<PublishPromptData, ThrowOnError>): RequestResult<PublishPromptResponses, PublishPromptErrors, ThrowOnError> => (options.client ?? client).post<PublishPromptResponses, PublishPromptErrors, ThrowOnError>({
    url: '/api/v1/prompts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One prompt: its labels, its versions, and what happened to it lately.
 */
export const getPrompt = <ThrowOnError extends boolean = false>(options: Options<GetPromptData, ThrowOnError>): RequestResult<GetPromptResponses, GetPromptErrors, ThrowOnError> => (options.client ?? client).get<GetPromptResponses, GetPromptErrors, ThrowOnError>({ url: '/api/v1/prompts/{name}', ...options });

/**
 * Point a label at a version. `production` is the one an SDK reads.
 *
 * A `PUT`, because moving a label twice to the same version is the same
 * world as moving it once.
 */
export const setPromptLabel = <ThrowOnError extends boolean = false>(options: Options<SetPromptLabelData, ThrowOnError>): RequestResult<SetPromptLabelResponses, SetPromptLabelErrors, ThrowOnError> => (options.client ?? client).put<SetPromptLabelResponses, SetPromptLabelErrors, ThrowOnError>({
    url: '/api/v1/prompts/{name}/labels/{label}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Record an optimisation and store its candidate.
 *
 * The verdict in the response is computed here from the held-out scores and
 * from what the candidate did to the baseline's variables — it is not read
 * from the request. An optimiser selected its candidate by maximising the
 * number it is reporting, which makes it the last thing that should grade it.
 */
export const recordOptimization = <ThrowOnError extends boolean = false>(options: Options<RecordOptimizationData, ThrowOnError>): RequestResult<RecordOptimizationResponses, RecordOptimizationErrors, ThrowOnError> => (options.client ?? client).post<RecordOptimizationResponses, RecordOptimizationErrors, ThrowOnError>({
    url: '/api/v1/prompts/{name}/optimizations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One optimisation, with the report the optimiser attached.
 */
export const getOptimization = <ThrowOnError extends boolean = false>(options: Options<GetOptimizationData, ThrowOnError>): RequestResult<GetOptimizationResponses, GetOptimizationErrors, ThrowOnError> => (options.client ?? client).get<GetOptimizationResponses, GetOptimizationErrors, ThrowOnError>({ url: '/api/v1/prompts/{name}/optimizations/{optimization_id}', ...options });

/**
 * Re-derive the prompt's index from the objects that are stored.
 *
 * The repair path, exposed because the thing it repairs — a head that lost a
 * concurrent write — is invisible until somebody notices a version missing
 * from a list. Labels survive it; a label pointing at a version that is gone
 * does not.
 */
export const rebuildPrompt = <ThrowOnError extends boolean = false>(options: Options<RebuildPromptData, ThrowOnError>): RequestResult<RebuildPromptResponses, RebuildPromptErrors, ThrowOnError> => (options.client ?? client).post<RebuildPromptResponses, RebuildPromptErrors, ThrowOnError>({ url: '/api/v1/prompts/{name}/rebuild', ...options });

/**
 * One version, with its text.
 */
export const getPromptVersion = <ThrowOnError extends boolean = false>(options: Options<GetPromptVersionData, ThrowOnError>): RequestResult<GetPromptVersionResponses, GetPromptVersionErrors, ThrowOnError> => (options.client ?? client).get<GetPromptVersionResponses, GetPromptVersionErrors, ThrowOnError>({ url: '/api/v1/prompts/{name}/versions/{version_id}', ...options });

/**
 * List runs, newest first.
 */
export const listRuns = <ThrowOnError extends boolean = false>(options?: Options<ListRunsData, ThrowOnError>): RequestResult<ListRunsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListRunsResponses, unknown, ThrowOnError>({ url: '/api/v1/runs', ...options });

/**
 * One run with the spans finished so far.
 */
export const getRun = <ThrowOnError extends boolean = false>(options: Options<GetRunData, ThrowOnError>): RequestResult<GetRunResponses, GetRunErrors, ThrowOnError> => (options.client ?? client).get<GetRunResponses, GetRunErrors, ThrowOnError>({ url: '/api/v1/runs/{run_id}', ...options });

/**
 * Read from the durable log rather than the read model: this is the audit
 * view, and it must show what was actually recorded.
 *
 * Which side it is on is read from the log too — from the run's **first**
 * event, which is the same rule the runs fold keeps (`RunSummary::project`,
 * first-seen-wins). Asking the read model instead would have left a hole with
 * a name: a project's run whose row had been evicted would become readable on
 * the global side, on the one route that answers past the read model.
 */
export const getRunEvents = <ThrowOnError extends boolean = false>(options: Options<GetRunEventsData, ThrowOnError>): RequestResult<GetRunEventsResponses, GetRunEventsErrors, ThrowOnError> => (options.client ?? client).get<GetRunEventsResponses, GetRunEventsErrors, ThrowOnError>({ url: '/api/v1/runs/{run_id}/events', ...options });

/**
 * Server-sent events for one run: history, a catch-up marker, then live.
 */
export const streamRun = <ThrowOnError extends boolean = false>(options: Options<StreamRunData, ThrowOnError>): RequestResult<StreamRunResponses, unknown, ThrowOnError> => (options.client ?? client).get<StreamRunResponses, unknown, ThrowOnError>({ url: '/api/v1/runs/{run_id}/stream', ...options });

/**
 * Every retained span, flat and filterable.
 *
 * The waterfall answers "show me this run". This answers "show me every tool
 * call slower than two seconds", which is the question asked when the run to
 * look at is not yet known.
 */
export const listSpans = <ThrowOnError extends boolean = false>(options?: Options<ListSpansData, ThrowOnError>): RequestResult<ListSpansResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListSpansResponses, unknown, ThrowOnError>({ url: '/api/v1/spans', ...options });

/**
 * What this deployment has configured, and which variable decides each.
 *
 * `admin`, because this is an inventory of the deployment rather than help
 * for whoever is reading a run.
 */
export const system = <ThrowOnError extends boolean = false>(options?: Options<SystemData, ThrowOnError>): RequestResult<SystemResponses, SystemErrors, ThrowOnError> => (options?.client ?? client).get<SystemResponses, SystemErrors, ThrowOnError>({ url: '/api/v1/system', ...options });

/**
 * Training runs, newest first.
 */
export const listTrainingRuns = <ThrowOnError extends boolean = false>(options?: Options<ListTrainingRunsData, ThrowOnError>): RequestResult<ListTrainingRunsResponses, ListTrainingRunsErrors, ThrowOnError> => (options?.client ?? client).get<ListTrainingRunsResponses, ListTrainingRunsErrors, ThrowOnError>({ url: '/api/v1/training-runs', ...options });

/**
 * Open a training run.
 *
 * Answered before the first epoch on purpose: if this instance is going to
 * refuse the run, a trainer should find out now rather than after six GPU
 * hours. Re-opening an already-open run returns it, so a retried start does
 * not lose the curve it already wrote.
 */
export const startTrainingRun = <ThrowOnError extends boolean = false>(options: Options<StartTrainingRunData, ThrowOnError>): RequestResult<StartTrainingRunResponses, StartTrainingRunErrors, ThrowOnError> => (options.client ?? client).post<StartTrainingRunResponses, StartTrainingRunErrors, ThrowOnError>({
    url: '/api/v1/training-runs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One run, with its whole curve.
 */
export const getTrainingRun = <ThrowOnError extends boolean = false>(options: Options<GetTrainingRunData, ThrowOnError>): RequestResult<GetTrainingRunResponses, GetTrainingRunErrors, ThrowOnError> => (options.client ?? client).get<GetTrainingRunResponses, GetTrainingRunErrors, ThrowOnError>({ url: '/api/v1/training-runs/{run_id}', ...options });

/**
 * Close a run.
 */
export const finishTrainingRun = <ThrowOnError extends boolean = false>(options: Options<FinishTrainingRunData, ThrowOnError>): RequestResult<FinishTrainingRunResponses, FinishTrainingRunErrors, ThrowOnError> => (options.client ?? client).post<FinishTrainingRunResponses, FinishTrainingRunErrors, ThrowOnError>({
    url: '/api/v1/training-runs/{run_id}/finish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One batch of progress: epochs, sampled points, checkpoints, profiles.
 *
 * Returns the summary rather than the whole record, because a trainer flushing
 * every epoch does not want its own curve back every time.
 */
export const recordTrainingProgress = <ThrowOnError extends boolean = false>(options: Options<RecordTrainingProgressData, ThrowOnError>): RequestResult<RecordTrainingProgressResponses, RecordTrainingProgressErrors, ThrowOnError> => (options.client ?? client).post<RecordTrainingProgressResponses, RecordTrainingProgressErrors, ThrowOnError>({
    url: '/api/v1/training-runs/{run_id}/progress',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Take one attempt, or say there was none.
 *
 * The server does everything up to the work itself: it claims the row, loads
 * the plan the run pinned, answers from the cache if an entry already covers
 * this key, and reports `step.started`. A 204 means there was nothing — or
 * that a cache entry settled it without anybody running anything, which is a
 * finished attempt rather than an idle one and looks the same from here.
 */
export const claim = <ThrowOnError extends boolean = false>(options: Options<ClaimData, ThrowOnError>): RequestResult<ClaimResponses, ClaimErrors, ThrowOnError> => (options.client ?? client).post<ClaimResponses, ClaimErrors, ThrowOnError>({
    url: '/api/v1/worker/claims',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Renew a claim.
 *
 * A 409 is the signal to stop rather than to try harder: the lease went, the
 * attempt has been taken over, and whatever this worker is in the middle of
 * will not be accepted.
 *
 * It is also how a cancel reaches a worker. A heartbeat for an attempt whose
 * run is cancelling, or ended around it, settles that attempt as stopped and
 * answers 409 `execution_stopping` — the pod launcher's rule for a pod and the
 * reactor's for a step in its own process — so a cancel completes at the
 * worker's next heartbeat rather than when its work would have finished.
 */
export const heartbeat = <ThrowOnError extends boolean = false>(options: Options<HeartbeatData, ThrowOnError>): RequestResult<HeartbeatResponses, HeartbeatErrors, ThrowOnError> => (options.client ?? client).post<HeartbeatResponses, HeartbeatErrors, ThrowOnError>({
    url: '/api/v1/worker/claims/{execution_id}/{step_id}/{attempt}/heartbeat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Read one of this attempt's inputs.
 *
 * Proxied rather than presigned. A presigned URL is a bearer credential for a
 * bucket that also holds prompts, datasets, annotations, conversations and
 * training, scoped by a prefix and a clock and handed to a process that may be
 * behind somebody's NAT; this route can check the one thing that matters,
 * which is that the caller holds the lease on the attempt whose input it is
 * asking for.
 */
export const readInput = <ThrowOnError extends boolean = false>(options: Options<ReadInputData, ThrowOnError>): RequestResult<ReadInputResponses, ReadInputErrors, ThrowOnError> => (options.client ?? client).get<ReadInputResponses, ReadInputErrors, ThrowOnError>({ url: '/api/v1/worker/claims/{execution_id}/{step_id}/{attempt}/inputs/{name}', ...options });

/**
 * Store rows this attempt produced, and get the pointer back.
 *
 * The digest is of the bytes this instance wrote and never of anything the
 * caller claimed — the prompt registry's rule, and what makes the reference
 * safe to accept back on the result route.
 */
export const writeOutput = <ThrowOnError extends boolean = false>(options: Options<WriteOutputData, ThrowOnError>): RequestResult<WriteOutputResponses, WriteOutputErrors, ThrowOnError> => (options.client ?? client).post<WriteOutputResponses, WriteOutputErrors, ThrowOnError>({
    url: '/api/v1/worker/claims/{execution_id}/{step_id}/{attempt}/outputs/{name}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Report what an attempt produced, and let the decider decide what follows.
 *
 * The server re-checks the lease, records the outputs and the cache entry,
 * and reports `step.completed` or `step.failed`. None of that is the worker's
 * to do: a claimant is the one party that cannot check its own lease, and a
 * cache entry must not be written by whoever benefits from it.
 * Redelivery of the same durable outcome is acknowledged from history without
 * restoring a lease. Different outcomes conflict; diagnostics and cache hints
 * do not alter an already committed result.
 */
export const report = <ThrowOnError extends boolean = false>(options: Options<ReportData, ThrowOnError>): RequestResult<ReportResponses, ReportErrors, ThrowOnError> => (options.client ?? client).post<ReportResponses, ReportErrors, ThrowOnError>({
    url: '/api/v1/worker/claims/{execution_id}/{step_id}/{attempt}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const listWorkflowDefinitions = <ThrowOnError extends boolean = false>(options?: Options<ListWorkflowDefinitionsData, ThrowOnError>): RequestResult<ListWorkflowDefinitionsResponses, ListWorkflowDefinitionsErrors, ThrowOnError> => (options?.client ?? client).get<ListWorkflowDefinitionsResponses, ListWorkflowDefinitionsErrors, ThrowOnError>({ url: '/api/v1/workflow-definitions', ...options });

export const registerWorkflow = <ThrowOnError extends boolean = false>(options: Options<RegisterWorkflowData, ThrowOnError>): RequestResult<RegisterWorkflowResponses, RegisterWorkflowErrors, ThrowOnError> => (options.client ?? client).post<RegisterWorkflowResponses, RegisterWorkflowErrors, ThrowOnError>({
    url: '/api/v1/workflow-definitions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const getWorkflowDefinition = <ThrowOnError extends boolean = false>(options: Options<GetWorkflowDefinitionData, ThrowOnError>): RequestResult<GetWorkflowDefinitionResponses, GetWorkflowDefinitionErrors, ThrowOnError> => (options.client ?? client).get<GetWorkflowDefinitionResponses, GetWorkflowDefinitionErrors, ThrowOnError>({ url: '/api/v1/workflow-definitions/{name}', ...options });

/**
 * Forget a workflow's schedule.
 */
export const clearWorkflowSchedule = <ThrowOnError extends boolean = false>(options: Options<ClearWorkflowScheduleData, ThrowOnError>): RequestResult<ClearWorkflowScheduleResponses, ClearWorkflowScheduleErrors, ThrowOnError> => (options.client ?? client).delete<ClearWorkflowScheduleResponses, ClearWorkflowScheduleErrors, ThrowOnError>({ url: '/api/v1/workflow-definitions/{name}/schedule', ...options });

/**
 * One workflow's schedule, or 404 when it has none.
 */
export const getWorkflowSchedule = <ThrowOnError extends boolean = false>(options: Options<GetWorkflowScheduleData, ThrowOnError>): RequestResult<GetWorkflowScheduleResponses, GetWorkflowScheduleErrors, ThrowOnError> => (options.client ?? client).get<GetWorkflowScheduleResponses, GetWorkflowScheduleErrors, ThrowOnError>({ url: '/api/v1/workflow-definitions/{name}/schedule', ...options });

/**
 * Set or replace a workflow's schedule, and optionally start it once.
 *
 * A workflow that does not compile is refused here rather than at nine
 * tomorrow, exactly as a pipeline is — the compiler differs and nothing else
 * does, which is why one function serves both.
 */
export const setWorkflowSchedule = <ThrowOnError extends boolean = false>(options: Options<SetWorkflowScheduleData, ThrowOnError>): RequestResult<SetWorkflowScheduleResponses, SetWorkflowScheduleErrors, ThrowOnError> => (options.client ?? client).put<SetWorkflowScheduleResponses, SetWorkflowScheduleErrors, ThrowOnError>({
    url: '/api/v1/workflow-definitions/{name}/schedule',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Executions, newest first.
 */
export const listWorkflowExecutions = <ThrowOnError extends boolean = false>(options?: Options<ListWorkflowExecutionsData, ThrowOnError>): RequestResult<ListWorkflowExecutionsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListWorkflowExecutionsResponses, unknown, ThrowOnError>({ url: '/api/v1/workflow-executions', ...options });

/**
 * One execution: its nodes, its declared edges, and the messages observed.
 */
export const getWorkflowExecution = <ThrowOnError extends boolean = false>(options: Options<GetWorkflowExecutionData, ThrowOnError>): RequestResult<GetWorkflowExecutionResponses, GetWorkflowExecutionErrors, ThrowOnError> => (options.client ?? client).get<GetWorkflowExecutionResponses, GetWorkflowExecutionErrors, ThrowOnError>({ url: '/api/v1/workflow-executions/{workflow_run_id}', ...options });

/**
 * Server-sent events for one execution: history, a catch-up marker, then live.
 *
 * Scoped by `workflow_run_id` rather than by run, which is what makes a
 * stage-per-pod workflow watchable: the pod that has not started yet is the
 * interesting one, and a subscription resolved to today's run ids would never
 * see it.
 */
export const streamWorkflowExecution = <ThrowOnError extends boolean = false>(options: Options<StreamWorkflowExecutionData, ThrowOnError>): RequestResult<StreamWorkflowExecutionResponses, unknown, ThrowOnError> => (options.client ?? client).get<StreamWorkflowExecutionResponses, unknown, ThrowOnError>({ url: '/api/v1/workflow-executions/{workflow_run_id}/stream', ...options });

/**
 * Every workflow the projection knows, most recently active first.
 *
 * Includes workflows nothing ever declared: a producer that has been setting
 * `workflow_id` since before there was a graph to draw still belongs in the
 * picker, with whatever shape its runs revealed.
 */
export const listWorkflows = <ThrowOnError extends boolean = false>(options?: Options<ListWorkflowsData, ThrowOnError>): RequestResult<ListWorkflowsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListWorkflowsResponses, unknown, ThrowOnError>({ url: '/api/v1/workflows', ...options });

/**
 * One workflow's declared shape.
 */
export const getWorkflow = <ThrowOnError extends boolean = false>(options: Options<GetWorkflowData, ThrowOnError>): RequestResult<GetWorkflowResponses, GetWorkflowErrors, ThrowOnError> => (options.client ?? client).get<GetWorkflowResponses, GetWorkflowErrors, ThrowOnError>({ url: '/api/v1/workflows/{workflow_id}', ...options });

/**
 * Ask the configured orchestrator to run this workflow again.
 *
 * `202`, not `200`: nothing has run yet. What comes back is an
 * acknowledgement, and the evidence that the rerun happened is the events it
 * publishes onto the same log as everything else.
 */
export const rerunWorkflow = <ThrowOnError extends boolean = false>(options: Options<RerunWorkflowData, ThrowOnError>): RequestResult<RerunWorkflowResponses, RerunWorkflowErrors, ThrowOnError> => (options.client ?? client).post<RerunWorkflowResponses, RerunWorkflowErrors, ThrowOnError>({
    url: '/api/v1/workflows/{workflow_id}/rerun',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The process is running.
 */
export const livez = <ThrowOnError extends boolean = false>(options?: Options<LivezData, ThrowOnError>): RequestResult<LivezResponses, unknown, ThrowOnError> => (options?.client ?? client).get<LivezResponses, unknown, ThrowOnError>({ url: '/livez', ...options });

/**
 * The process can serve traffic.
 *
 * Distinct from liveness: a projector replaying a backlog is alive but not
 * ready, and restarting it would make the replay start over.
 */
export const readyz = <ThrowOnError extends boolean = false>(options?: Options<ReadyzData, ThrowOnError>): RequestResult<ReadyzResponses, ReadyzErrors, ThrowOnError> => (options?.client ?? client).get<ReadyzResponses, ReadyzErrors, ThrowOnError>({ url: '/readyz', ...options });
