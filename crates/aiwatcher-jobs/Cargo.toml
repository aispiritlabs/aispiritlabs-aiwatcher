[package]
name = "aiwatcher-jobs"
description = "The staged-artifact job primitive: a lease, a cursor, shards written before the cursor that passes them, and the content address a finished job is named by"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
# The version of a finished job is a content address over its shard digests,
# and it has to agree with `hashlib.sha256` on the reader's side for the same
# reason `PromptVersionId::of` does.
sha2.workspace = true
time.workspace = true
utoipa.workspace = true

[lints]
workspace = true
